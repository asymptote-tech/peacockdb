#0 and #2 are the stubs the key project and the per-batch join took for slots nothing else filled; #5 is the finish join's.
#[allow(clippy::too_many_arguments)]
#[allow(clippy::too_many_arguments)]
#[allow(clippy::too_many_arguments)]
#[cfg(not(feature = "rust-only"))]
#[cfg(not(feature = "rust-only"))]
#[cfg(not(feature = "rust-only"))]
#[cfg(not(feature = "rust-only"))]
#[cfg(not(feature = "rust-only"))]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[cfg(test)]
#[derive(Clone)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug)]
#[derive(Debug, Clone)]
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Clone, PartialEq, Eq)]
#[derive(Debug, Default)]
#[derive(Default)]
#[derive(Default)]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[test]
#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
#[tokio::test]
(build ordinal, probe ordinal) per key, in the order the join hashes them.
-- cross-lane --------------------------------------------------------------
-- determinism -------------------------------------------------------------------
-- lane-scoped -------------------------------------------------------------
-- queues ------------------------------------------------------------------
-- reporting ---------------------------------------------------------------
-- schedule ----------------------------------------------------------------
-- shuffles and routing ----------------------------------------------------------
-- the differential test ---------------------------------------------------------
-- the join hold -----------------------------------------------------------------
-- wiring ------------------------------------------------------------------
-- wiring ------------------------------------------------------------------------
0 unload, 1 join, 2 coalesce, 3 build source, 4 filter, 5 probe source.
0 unload, 1 merge, 2 emit, 3 source.
0 unload, 1 merge, 2 source.
0 unload, 1 merge, 2 source.
0 unload, 1 project, 2 filter, 3 source.
100 MiB — sits in the gap between the corpus' top query (tpcds q78 ≈ 135.5 MB) and the next one down, so the OOM tests get a real boundary to cross.
12 GiB.
1234567890123456789012345678 needs the high word, so a writer that dropped it would still produce a plausible small number.
2 GiB.
20 rather than 12 because the mock's limit forwards whole batches: trimming a mid-plan interval is the executor's job, so the number here means "not nothing".
70 GiB.
@2 is a column of the input and not of the intermediate table the finalize reads, which is the whole reason the two schemas are separate arguments.
A Left form emits its unmatched build rows in the same pass, so it cannot stream: #136's finish trick accumulates keys and a predicate join has none.
A Left join that answered per batch would pad it as unmatched against the second and emit the row twice.
A `BatchAccumulator` node's executor, one variant per node.
A `BatchAccumulator` node's executor, one variant per node.
A `GpuLimit` streams whatever is set here, because that is what a limit is — see `executors_for`.
A bare Partial aggregate as the root.
A bare column asks its input; an operator asks its operands; anything this does not model says yes.
A bare column reference, named from the schema at that position — the same pairing of ordinal and name the plan text carries, and for the same reason (#135).
A batch and the figure it was accounted at.
A batch enters a driver-held queue.
A batch entirely outside the interval is released where it stands, one entirely inside is forwarded untouched, and only the two that straddle its ends are sliced.
A batch is never larger than what the source holds — a scan pruned to a few row groups is where that is orders of magnitude.
A batch leaves a driver-held queue: consumed, handed to the caller, or dropped.
A batch outside a root-adjacent interval is released without a call.
A batch outside the interval, released where it stood.
A batch size below this is not worth deriving: the mapping is quantized to whole row groups anyway, so the model would be pretending to a precision the plan cannot use.
A batch that is a handle and a row count, as `GpuBatch` is — the second backend exists to differ from the first here, which is what the generic code must absorb.
A bound that only ever ran at one layout is a bound about one shape of arrival: only a streamed lane accumulates.
A budget under the two together is refused BEFORE the call, which is what the pre-check is for — the alternative is both live at once and nothing having said no.
A build side over a scan is its input's rows; an aggregate's state rests on the cardinality estimate, which today says one row per input row, so refusing on it would turn "we do not know" into "you cannot run this".
A build side that ended with no batch is a call of its own rather than an error: the scatter gave this lane no build rows, which a small table over many lanes produces routinely, and what the lane owes is the join type's answer.
A build-preserving join on the frozen surface also holds the key columns of every probe row it has seen, per lane, until the finish pass — the term that decides whether such a plan fits (#136).
A call against a recipe-plan node.
A call blocks a thread on one node's stream, and a sort past its in-place threshold spawns onto the runtime from under that block.
A call failed.
A call that fails ends the query.
A call whose modelled scratch came in under what it measured.
A cell where one keeps probe keys and the other makes a single call is a residual dropped or a finish pass that never runs, and neither shows up as a failed call — so this goes red for whichever cell a later reader gets wrong rather than for the ones we thought to write down.
A chain of nodes is as deep as it is long, and the C++ verifier refuses a plan past its depth limit at `begin_plan` — the whole query, before any call, which is #169.
A chain root -> ... -> leaf, one lane each.
A child of the right schema and nothing else:
A child that declares a schema and nothing else: an executor is handed its node and its input batches, so a stub is what keeps a case about the operator.
A coalesce above every source turns many batches into one, so every consumer sees a different arrival pattern for the same rows.
A coalesce and a cross join carry their inputs and nothing else, which is the whole content of the collapse arm: it concatenates whatever it is handed.
A coalesce carrying a fetch is not one of them — that is a limit, and it stops the walk.
A compaction runs exactly what done runs, which is what makes the doubling threshold a scheduling decision rather than a second computation.
A compaction that did not shrink will not shrink next time either — the keys are simply distinct — so the threshold moves to twice what is now held rather than re-scanning it per arrival.
A concat and a merge per compaction and again at done, plus the finalize project where this node finishes the aggregate.
A conscious decision per DataFusion node kind: nothing is carried over implicitly and an unrecognized node is a plan-time error naming it.
A context whose partial aggregates never stop aggregating.
A copy of it, because the call below consumes it and something else needs it too.
A copy of the build side, because the next probe batch needs it as well.
A copy of the probe batch, which the ABI cannot make either.
A count DataFusion answers from parquet statistics is a PlaceholderRowExec and a projection, with no aggregate to translate — refused by name until #158 settles which way out to take.
A count DataFusion can answer from parquet statistics never reaches an aggregate node at all, so the predicate is what makes this a real aggregate.
A count merges by SUM — the one place naming the merge separately from the init is the difference between a right and a wrong answer.
A count merges by SUM.
A cross join and a nested-loop join both emit rows built from a build row, the Left form's padding included, so an empty build side is an empty answer.
A crossing before it is a message: the accountant holds no names, and the driver formats one only on the path that ends the query.
A decimal NULL is what a stddev's finalize substitutes, so the precision and scale have to survive a value that has no digits.
A decimal scalar prints as its value, not as the triple its `Display` gives: a plan reader compares a literal against the column beside it.
A device label is `tp<N>-<memtier>` (e.g.
A device never skips, so this is also what keeps the two engines' answers the same.
A failed call ends the query: nothing further is scheduled and no handle is touched again.
A false positive costs a refusal a reader can see; a false negative is the wrong answer this exists to prevent.
A fetch does not:
A fetch legitimately overruns the batch it straddles, and an offset past the end names no rows rather than a negative count.
A filter drops rows and keeps columns, so declaring a decimal where its input holds an integer is a claim about a column nobody produced.
A filter that keeps nothing still owes a batch, because the node above counts calls and not rows.
A filter that projects re-selects columns rather than carrying them; its projection is checked by width above and by ordinal at the node.
A filter's selectivity, an aggregate's group count and a join's cardinality are all the trivial estimates the planner has today (#19, #73), and each of them can only overstate what its subtree really produces.
A filtered semi join is one legacy call over the whole probe side, so the planner owes it a single batch there — the capability matrix is what says so.
A finalize over a grouping-set state: the id is a key, so the project carries it through like any other.
A finalizing merge adds the project that carries the finalize — ours, so the two engines evaluate the same expression rather than agreeing because two implementations happen to match.
A finalizing merge answers for whole groups, so every row of a group has to be in this lane: either there is one lane, or the shuffle keyed on a subset of the columns being grouped.
A finished executor stops contributing.
A forwarder has no executor at all — the driver owns its rotation.
A forwarder lane can step when a source has a batch, or when the last live source has finished — the visit that retires it is what marks the lane done.
A fresh instance set per call, so the driver instantiates per lane; `lane` is needed because a loader's lane picks its own row groups out of the partitioner's mapping.
A global aggregate's lane can be empty — a mid-plan limit that dropped every batch of the one lane is how — and what it owes then is its identity row rather than no row.
A global one owes a row even for an empty lane; a grouped one owes no groups.
A group key cannot become NULL by being grouped on — except where a grouping set substitutes one deliberately, which the plan already carries as null_exprs.
A group with count <= ddof has no dispersion to report, so it is NULL rather than a division by zero or a root of a negative.
A grouped merge over no arrivals owes no groups, so nothing is also its answer.
A grouping set drops it the same way — it substitutes NULL for the keys it excludes, so the rolled-up rows are no longer where the hash put them even though the column is still in the group list.
A grouping set substitutes NULL for the keys it excludes, so any key some set drops can be NULL in the output — and the grouping id itself never is.
A grouping-set expansion emits `__grouping_id` beside the keys, which is a key everywhere above it.
A guard has to prove it was reached: the loop skips every state `can_step` refuses, so a category narrowed to never step would empty its own 32 states in silence.
A guard that cannot go red is not a guard, and a plan that violates one of these is unreachable from sql precisely because the translation layer is what inserts the fix — so a hand-built input is the only thing that can show the guard working.
A handle an FFI call consumed must skip that drop:
A handle an FFI call consumes is gone by move, and one this executor is sitting on is a `GpuBatch` — so a lane the driver abandons releases what it held rather than leaking it.
A handle to a resident `cudf::table`, plus the session it belongs to.
A hash aggregate answers in whatever order its table holds; every claim here is about which group got which value.
A hash is made by exactly one node and an order by three, so a claim with no such node beneath it was minted rather than carried — the shape I4 was: a join deriving `ByHash` from its keys and its column names, over lanes nothing had scattered.
A hash repartition is the shuffle: merge the lanes into one, then scatter that one into N by the same murmur3 both engines use.
A hashed column that is not grouped on takes the claim with it: rows of one group would then be spread across lanes, which is exactly what the co-location rule above exists to refuse.
A join before its build side arrives.
A join before its build side arrives.
A join does not move a row between lanes, so a claim survives — but only the claim of a side whose rows are never padded, and only where that side earned it by being hashed on this join's keys.
A join filter's reference, resolved onto the side it came from and that side's own ordinal — `k@build:0` is column 0 of the build child, whose schema is on its own line.
A join holds its build side and is handed a probe batch per lane on top of it; every other accumulator's held already is what it received.
A join holds its probe subtree from time zero: no lane has left build yet, and a probe batch produced before `set_build` has nowhere to go.
A join lane driven to its probe phase, with the build batch already delivered.
A join lane whose build side ended with no batch — its scatter gave it no build rows.
A join that answers in one call has no finish pass, so what its per-call type would be is a question with no answer — asking it is the defect, and the `unreachable!` behind it is doing its job.
A join whose build side ended empty owes nothing, and it reads its probe side anyway to let go of it — a dropped batch is a consumed one, so the identity holds here too.
A join whose build side was empty and which owes nothing: its probe batches still arrive, and the lane reads them only to let them go.
A join whose projection is dropped declares the projected columns and emits every one it crossed, which shifts every reference above it.
A join with its build side set, taking probe batches.
A join with its build side set, taking probe batches.
A join's build side is always one batch, and the planner is what makes it one.
A join's two sides are scattered by two different emitters, and lane `p` of one has to hold the rows that can match lane `p` of the other.
A join, as the schedule sees one: the range of its probe subtree, and how many lanes have yet to leave their build phase.
A join, so the walk has a node with two children: every order agrees on a chain.
A k-way merge reads one row per input at a time and takes the smallest, so the order it merges on has to be the order its input's batches already carry: merging on a key the batches are not sorted by emits rows in no order at all — and then declares them sorted, which is the claim a top-N above it acts on.
A key pair is one ordinal into each side.
A knob rather than the default: a mock that always prices honestly cannot express a model that stays silent, and a silent model is what lets a peak cross a budget uncaught.
A lane builds on its first step, not before, and an eager build is invisible without a count.
A lane never entered has not been, so it counts.
A lane that never runs must cost nothing: at four lanes over two row groups, two of them have no work and building eagerly would allocate for both.
A lane that produced nothing is an empty list at its own position, not a lane the file leaves out: the position is what pairs a lane with its sibling numbers.
A lane that received nothing usually owes nothing — but a global aggregate owes its identity row, `count 0` rather than no row, and a mid-plan limit that dropped every batch of its one lane is how that lane comes to be empty.
A lane the mapping does not have is a plan the partitioner did not produce, and the message says which lane against how many.
A lane the mapping gave nothing is exhausted on its first step rather than an error: the partitioner is free to produce one, and the driver's answer is an empty lane.
A lane whose build side ended with no batch, for a join that owes nothing without one: the lane ends, its probe batches are released where they stand, and the run completes.
A lane whose probe was empty accumulated no keys, and the concat of none is a refusal on this side (#173).
A lane whose probe was empty — routine above a shuffle, and the shape where the two backends had no answer between them.
A lane whose scatter gave its build side no rows: routine for a small table over many lanes, and the executor is asked what the type owes rather than the driver assuming the plan is at fault.
A lane with no batches is not a defect — four lanes over two row groups leave two of them empty, and the mapping says so rather than inventing work.
A lane's batches concatenated into one at done, and nothing at all where none arrived.
A lane's batches concatenated into one at done.
A lane's batches ordered into one at done.
A lane's end carries no batch, so there is nothing to release: counting one anyway made holds and releases disagree on every plan with a cross-lane accumulator, which is what the report says they never do.
A lane's reads, in the order the mapping named them.
A lane's reads, in the order the mapping named them.
A lane's worth of arrivals through one accumulator, and what it answered with at done.
A leaf would prove nothing here — the counts agree either way when nothing was taken.
A limit DataFusion pushed into the scan is the whole answer wherever it erased the limit node above it — `SELECT * FROM nation LIMIT 3` at tp4 plans as a bare scan carrying limit=3.
A limit holds nothing, which is the whole point of the slice symbol.
A limit holds nothing, which is the whole point of the slice symbol.
A limit is a prefix of its stream: it neither increases the batch count nor disturbs an order, so the layout is its input's.
A limit only saves what it stops being read, and by the time an accumulator emits it has already built its whole state — so an interval at the sink above one arrives too late to have prevented anything.
A limit pushed into the scan by DataFusion, not one this mode derived.
A limit that is held without being marked done leaves the parent waiting for a lane that has in fact finished, and the query answers nothing.
A limit's `skip`/`fetch`, carried by whichever node owns the interval: a mid-plan `GpuLimit`, or the `GpuUnload` that absorbed a root-adjacent one.
A limit's interval, whichever node carries it.
A literal as the wire holds it: the value, and for a decimal the precision and scale that decide what the value means.
A loader with `lanes` lanes.
A local path, since the mapping is read off the file here at plan time.
A mark join's finish emits the build side AND the boolean it appends, so its projection indexes one column past the build side.
A mask is one per set in key order and so is as long as the group list, but a short one reads as excluding: over-claiming co-location is what this function exists to avoid.
A merge groups on what the init emitted — keys and, where there was one, the grouping id — and expands nothing: the sets were expanded once, below.
A merge over batches nobody sorted.
A merge over one lane is not a no-op to be optimized away later — it is a node this plan does not have, and the golden shows it as one.
A merge reads the state annotation rather than re-deriving where the state is: for each aggregate its input declares, the columns at the declared positions must be merged, and merged by the aggregators that aggregate's decomposition names — a count merges by sum, and a Welford triple merges as one call.
A merge sums a column that is already a sum, and DataFusion widens a decimal at every such step — so a state merged twice would carry a wider type than one merged once, and what a lane emits would depend on how many batches it saw.
A merge takes the smallest head row of each input, so on the wrong key it emits rows in no order and then declares them sorted, which is what a top-N above it acts on.
A merge that turns 40 rows into 7 says so on its own line: the fetch rides the merge, and a reader who found it only on the sort beneath would misread which node truncates.
A mid-plan limit holds nothing between calls, whatever its offset — which is the whole point of the slice symbol: the rows before its start are released where they stand rather than accumulated until the interval opens.
A mid-plan limit is a `BatchAccumulator` by category and holds nothing at all, which is why it is not one of these.
A mid-plan limit makes the same three-way choice inside its executor, so for that one the driver only counts.
A mid-plan limit:
A model reading the budget's target instead would say the same number for all three.
A move between queues: the batch stays in flight, so nothing is accounted.
A name as a single token: bare where it already is one, in backticks where it holds a character this rendering punctuates with — whitespace, a comma, a bracket, an `@`, or a backtick, which doubles.
A name is not always an identifier:
A nested operator is parenthesized, so precedence is read off the line.
A node of the recipe plan, addressed by its position in it.
A node that declared its child's schema here would emit an extra column and shift every ordinal above it.
A node that makes no ABI call gets no recipe, and the absence is a statement about the node rather than a gap: a forwarder routes batches and touches no device.
A node that makes no ABI call says `none` rather than nothing: "this node touches no device" and "the pass missed it" have to look different, and a blank line makes them look the same.
A node that moves rows rather than changing them emits its input's columns, so the two schemas must be the same fields and not merely the same count.
A node the plan did not ask for, in two kinds.
A node the registry does not know is a hand-built one under test, and declares none of the parameters this rule reads.
A node whose kind the test writes: the structural rules are about what a node declares of itself, and the translation layer is what makes those declarations agree with the schema — so a plan that breaks one is unreachable from sql.
A node with no `final` emits its state; one with a `final` emits the finalized columns, and that is the only thing distinguishing the positions — the single-node shortcut is init aggregators and finalize expressions on the same node.
A node's calls, in order — the batch into the first, each output into the next.
A node's declared column count against the parameters that produce those columns.
A node's identity as a number, which is what a recording can hold and compare.
A node's operators in call order, each one's output the next one's input — one for a filter, two for an aggregate that finalizes, which is the recipe's own call list on the other backend.
A node's own [`validate_schemas_and_partitions`](GpuNode::validate_schemas_and_partitions) judges what its children hand it and can name the node that fixes it.
A number in the type an aggregate declares it outputs.
A number per node kind, from an exhaustive match, so the hand-written list below is answerable: a variant added to `NodeRef` fails to compile here rather than going quietly untested.
A payload that cannot be written fails the whole plan, and the seq it would have taken is in the message.
A per-batch sort's top-N, applied by slicing what the sort ordered.
A plan that exists is one every seq of which holds the kind its recipe claims — [`attach_recipes`] fails rather than substituting anything for a payload it cannot write, so there is no second accessor and no caveat to remember.
A plan that violates what a node requires of its children.
A predicate answers true or false about a NULL rather than becoming one.
A predicate rather than a key:
A prefix is enough: batches sorted on `[a, b]` merged on `[a]` come out ordered by `a`.
A prefix of the keys would still hold, but a sort key that vanished mid-list leaves an order nothing downstream can name.
A probe batch for a lane that has no build side: released rather than probed, since what it could have matched is not there.
A project built from expressions the caller already wrote — the join's key and pad projects come this way, since neither is a plan node of its own.
A project list, in the shape `ProjectionExec` takes.
A project replaces the row, so the finalize list alone would answer with the finalized columns and no keys to read them by.
A project replaces the row, so the finalize list alone would answer with the finalized columns and no keys to read them by.
A project, an aggregate and a union in one plan: the sum's declared type is DataFusion's widening of the column it sums, the project renames without retyping, and the branches meet a union that declares the same type both carry.
A projected ordinal indexes the table the join emits before its own projection — both sides for the four that pair rows, one side for a semi or anti join, and that side plus the mark for a mark join.
A projection index with no column chunk would silently make the source look smaller, and bytes decide both the batch size and the lane count.
A projection is ordinals into the joined table, so it is named from both sides rather than printed as bare positions.
A projection re-numbers columns and can compute new ones, so each is its own question; a filter's projection is the same question over a subset.
A projection that keeps two of the four columns, one from each side.
A protocol violation is about one lane of one node, and the lane driver knows neither: without this the message names a shape and no site.
A protocol violation no type can reach: a build side that produced zero or two batches, an `emit` returning other than N, a step after finishing.
A pure function called twice cannot show determinism, so what this asserts is the mapping itself: a policy change moves it.
A pure offset never satisfies, so every batch past the skip is forwarded whole.
A pure offset of zero forwards every batch and never satisfies, by the lowering rule.
A query that ended badly still gives back what it was holding, by the same path the early exit uses: no handle is touched again, but the totals still reconcile, so held and released stay equal on every path out of here.
A range naming no rows exports nothing at all, and there is nothing to free.
A real consumer above it, and not one that vanishes: the interval is mid-plan only when something above it goes on reading, and this is also what makes the tree canonical — the planner refuses a limit whose only parent is the sink.
A real one may add its own state — the accountant's unit tests cover that — but keeping state out here is what lets a test trip the post-check without the pre-check firing first on the same figure.
A recheck is this against the holds, never a sweep over lanes.
A recipe function is handed its node and the schemas that node declares it consumes, so that is all a case needs to build — and a stub is what keeps a case about the join rather than about a subtree.
A recipe names one only where a later call in the same probe still needs that batch — the key project of a Left or Full join, with the per-call join reading the batch after it — so there is no arrangement of these two calls that leaves both a batch to read.
A recipe that names a different pattern or a different input is one this executor would drive at the wrong moment, which is a wrong answer rather than a failed call.
A recipe-plan node under construction: which kind it is and its union payload.
A refresh takes the figure after the call, never a handle to what produced it.
A refused call and a failed one both return nothing, so the rows cannot tell them apart and the trace is what does.
A rename for the five simple aggregates, a divide for `avg`, and a `CASE` over a `sqrt` for the Welford pair — all of them ordinary IR, which is what replaces the hardwired `avg_div` and `std_finalize` arms.
A rename for the five simple aggregates, a divide for `avg`, and a `CASE` over a `sqrt` for the Welford pair — all of them ordinary IR, which is what replaces the hardwired `avg_div` and `std_finalize` arms.
A residual filter is not decoration: it decides whether a shape streams its probe, drops to one legacy call over a probe the planner made single-batch, or is refused outright — so every cell above has a twin here.
A residual filter takes the build-side semi family off the streaming path, and the planner puts a GpuCoalesceAllBatches under the probe: one node, one call, no finish.
A row group over target is still its own batch — one row group is the minimum granularity, and the planner always emits a plan (the accountant owns the runtime consequence, #142).
A run that drained mentions it nowhere, which is what keeps the common section as it was: five queries in the corpus stop early, and every other node of every other query would otherwise carry a list of zeroes.
A run that ended because a limit was satisfied legitimately leaves lanes not done and queues non-empty; any other one has to have drained.
A run that ends any other way without a limit having been satisfied has lost work rather than finished.
A run-level trip carries a phase, a node and a lane, so all three are asserted here as well as the thing that distinguishes the two checks: the call never ran.
A satisfied limit does not break the identity at the node that carries it: every batch that reached it was consumed, and it stopped by not being scheduled again rather than by leaving one behind.
A scan of nothing, holding a slot open: the second rule, since an empty slot is a crash at plan load rather than an absent child.
A scatter holds nothing: its N outputs are the batch it was handed, redistributed.
A scatter holds nothing: its N outputs are the batch it was handed, redistributed.
A scatter rather than a `RepartitionExec` because that node has N output partitions and the per-node relay drives one.
A schema of one column, of the type given: the pair below is Int64 against Decimal128(15,2), because a decimal read as its neighbour is what these rules exist to catch and what no per-node byte count can show.
A script says what a join owes with no build side, since a mock join has no type.
A semi join emits the build side alone, so @2 is past its table even though the two sides hold four columns between them — the bound that reads both sides would let it through.
A semi join owes no rows and a mark join owes a column of `false`, and neither is a table the frozen surface makes out of nothing.
A semi join with no projection publishes no narrowing call: the finish already emits the row, and a project that keeps every column is a call for nothing.
A semi join's output is the build side, so its projection only narrows — and publishing no call for that narrowing is what left the device emitting every build column while the CPU emitted what the node declared.
A semi or anti join is a filter written as a join — it emits the side it decides about — and a mark join emits that side plus the boolean it computed.
A seq is a post-order index into the tree the C++ indexes, so it cannot be counted from the recipes alone: whatever fills a structural slot is indexed too.
A seq that resolves to nothing means the tree is shorter than the numbering — something the walk built was left unreachable, and every seq above the gap addresses the wrong node.
A seq that resolves to the wrong kind means the claim and the buffer disagree about what a call runs, which no golden would catch: both are rendered from the same recipe.
A session configured to give up on grouping as early as it can: the probe after one row, and any aggregation ratio above zero enough to trigger it.
A session whose sort cannot stay in place, so `ExternalSorter` takes the arm that spawns instead of the one that concatenates and sorts once.
A shape from parents in pre-order.
A shape the engine does not implement — window functions (#143), a mixed distinct (#62), a value-form CASE (#57).
A sink declares no layout of its own, so it runs the lanes its input hands it.
A sink declares no layout of its own: it calls once per lane of its input.
A sink declares no schema of its own, so this is its input's — the columns that cross the boundary.
A sink holds nothing of its own; its input's lane count is that node's.
A sink is the root, so it is never one.
A sink structurally has no layout and no schema; everything else always has both, which is why they live inside the kind rather than beside it as two `Option`s that have to be `None` together.
A site over a real node of the right kind, since `executors_for` reads the node.
A skipped prefix is consumed and never emitted — the batch is released where it stands, so it is in `consumed` and in no `emitted` list.
A sort becomes a per-batch sort plus the accumulator that makes the whole stream ordered, and the `fetch` is replicated onto both: the top n of a union is the top n of each part's top n, which is what keeps a top-N memory-bounded.
A sort over a plain scan holds the whole table, and no estimate stands between that number and the truth — which is the shape of a build side larger than vram.
A sort past its in-place threshold spawns onto the runtime from under that block, which is what a driver has to leave room for (T17).
A sort per batch, then one merge over the runs at done.
A sort's per-batch half, before the parent decides which accumulator goes above it.
A source holds nothing between calls: the batch it reads is its output, and the reader it opens lives inside one call.
A source holds nothing between calls: the handle a read produces is its output, and the row groups it still owes are a list, not a table.
A source is its own input.
A source reading fewer bytes than this plans one lane whatever the target, and the nodes a one-lane region does not need are then not emitted at all.
A source reading less than this stops being worth splitting: it has nothing to gain from lanes and would pay a shuffle for them.
A source's amplification: the largest its batch gets anywhere on the way to the accumulator that ends the walk, counting the lanes live at that point.
A source, since a leaf that is not one is itself a finding below.
A state batch of `ROWS` rows under keys the caller names.
A state schema in the shape an aggregate declares one: the keys lead it, and the annotation says which columns a Welford triple owns.
A state schema whose annotation says where the Welford triple sits: the columns, the keys, and the one aggregate the last three of them belong to.
A state-shaped input is read positionally, by a cursor walking each aggregate's state width, so an aggregate listed out of order reads another one's columns.
A stddev's finalize, whole: the shape the fifth operator was appended for.
A step neither moves a batch nor finalizes a lane only if the schedule is wrong, so a run that does not end is a bug here rather than a query that is merely large.
A streamed probe cannot know which build rows matched, so the types that owe their build side a row keep the probe keys per batch and answer once at done (#136).
A stub input over a state schema, which is what a merge reads.
A stub input with a layout stated, since a join's two sides differ in exactly that.
A stub input with the layout a join requires of that side: the build is one batch per lane, the probe streams.
A stub is numbered here, before the node that takes it, for the same reason.
A sub-expression that is itself an operator is parenthesized, so precedence is read off the line rather than assumed.
A test on the rows returned passes just as well when the whole input was read and trimmed at the end, which is the thing this lowering exists to avoid.
A third `Backend` instantiation, alongside the CPU and GPU ones: scripted counts, sizes, skew patterns and accumulator behaviour, and no operator that computes anything.
A top-N holds n rows per lane, which is what makes it bounded.
A trip carries no name, and the driver is what can supply one — so every step returns this and the driver renders it at the one place a query ends.
A trip is a clean query failure, and what it promises is narrow: a check that sees an accounted total over budget fails the query.
A typed NULL literal, which is what pads a probe column an anti join never emitted.
A typestate: build -> probe -> done, each transition consuming the last state.
A visit to output lane p cycles `sources_of(p)` in listed order, forwarding one batch per visit, skipping sources with nothing queued and retiring those whose producer has finished — the merge's round-robin and the interleave's per-lane child rotation are that same rule.
A whole-stream order is `BatchSorted` meeting `SingleBatch`, derived by [`PartitionLayout::is_stream_sorted`] rather than declared, so there is no second way to say it.
Above any single batch and below what the accumulator builds out of them, so the trip cannot be a batch in flight: it is the state, and the node named says so.
Absolute local path of the single physical file backing a ParquetExec, or `None` if the scan touches more than one distinct file.
Account a call's outputs and release its input, in the order the spec states: the input is alive while the call runs, so it leaves the resident set after it.
Accumulates a lane's sorted batches and merges them into one at done, so its output is stream-sorted rather than batch-sorted.
Add bloom pruning here too if that day comes.
Adding a node is one line here and an exhaustive match everywhere it is consumed — the renderer, a backend's executor match, the serializer — rather than a downcast chain per consumer.
All row-group indices `0..N` of a single-source ParquetExec (no pruning).
All three take their build side as one batch per lane and stream the probe where the capability matrix allows it; the equi-join is co-partitioned, while cross and nested-loop need both inputs in one lane, having no key to co-locate on (#140).
All-zero prints as `0 days`, since an interval of nothing is still an interval.
Also no seq, and for the same reason: the row range the sink exports is counted across lanes at run time.
An Inner join, whose probe call does read the build side, so the charge is right there.
An N-into-1 order-preserving merge: the per-lane sort below it stays a per-batch sort, and this node is the accumulator, so no per-lane accumulator is emitted.
An `ExecutionPlan` that hands out one pre-built stream.
An address rather than a name, because a plan holds several nodes of a kind and a name would pair a build with the wrong one of them.
An aggregate node carries no phase: it declares aggregators over its input and, where it finishes the aggregate, one expression per output column.
An aggregate state annotation names the columns it decomposed into, so those columns have to be there: a merge reads the positions rather than re-deriving them, and a position past the output would read whatever the next node emits at that ordinal.
An aggregate's finalize, which is ours rather than the executor's: both engines evaluate this expression, so they agree by construction.
An aggregate's own output can be NULL: a sum over no rows is.
An aggregate's state is one row per input row only because there is no cardinality estimate (#19); tpch q1 groups six million rows into four.
An aggregator owns as many state columns as it emits — one each at the init, three for a merge's `merge_m2` — so which aggregator a state position names is a walk over those widths, never `position - group_width`.
An annotation nothing checks is a comment.
An anti join owes every build row: nothing matched, because nothing arrived to match.
An early exit stops the schedule, and whatever sits in a queue at that moment is released rather than consumed — so the node that falls short is wherever the stop caught one, which is not the node carrying the limit.
An emitter is the node the identity is worth having on: it reads one lane and writes four, so consumed indexed by its own lanes could not be compared to anything.
An emitter scatters one lane into N and a partition accumulator merges N into one, so neither calls per lane whatever its output declares — which is why the section spells this `calling_lanes` rather than reusing the tree's `lanes`.
An empty `underestimates` means the model held only where this is not zero: a backend reporting `None` everywhere produces the same empty list for want of an input.
An empty lane emits nothing, on this backend as on the device — the collapse of no handles is a refusal there (#173), so a batch invented here would be a row the other engine cannot produce.
An empty name is what a reference past the end renders as, and validation is what refuses the plan.
An enum rather than a label: this is written once per call, and a per-call format is a cost the trace does not need to impose.
An equality on every run, with no exception keyed on node kind — the two that once looked like exceptions were measured and are not, and each is pinned below.
An equi-join: the build side is one batch per lane, the probe streams unless the capability matrix says otherwise, and lane p of each side holds exactly the rows that can match lane p of the other.
An exec node holds nothing between calls, and its transient is the answer it is about to build out of the batch it was handed.
An exec node holds nothing between calls; its transient is the table the call is about to build, which is bounded by the batch it reads.
An expression can be NULL unless every path to a value is known not to be.
An expression with the name its column takes in the node's output — a project list entry, or one column of an aggregate's `final` list.
An input declaring one aggregate's state at the positions given, as a partial's output is `[group key, state columns…]`.
An input whose layout and schema the test writes.
An input with a layout and schema chosen by the test: the guards below are about what a node requires of its input, and a plan that violates one is unreachable from sql precisely because the translation layer is what inserts the fix.
An object store's location would simply not open, which is an error rather than a wrong answer.
An oracle cannot answer that differently from run to run.
An order on no columns is no order, so it canonicalizes — otherwise two layouts that mean the same thing compare unequal.
An ordinary reference indexes the node's input, which is the line below it.
An out-of-range one does not merely read the wrong column:
An outer join null-pads the side it does not preserve.
An outer join's does not — its finish is an anti join over the build side — so the probe columns it owes are typed NULLs and the project is always needed.
An unload of part of a batch — the interval straddles one of its ends.
An unload takes any lane count and any batch layout — its interval is counted across lanes by the driver rather than requiring a merge below it — but an interval it absorbed names rows in the input's order, so the same prefix rule applies here as on a mid-plan limit.
Any satisfied node at all — the run ended early, so in-flight batches are dropped rather than reported as stranded.
Arrow's `get_array_memory_size` is what was ALLOCATED: 64-byte aligned buffers and an absent validity bitmap where the device counts one, so the two engines put different numbers on one node of one query.
Arrow's own rendering, minus the noise a plan reader does not need.
Arrow's safe cast answers a value that does not fit with a NULL, and a NULL in a sum column cannot be told from one the data had — so this path uses the unsafe cast and the query ends instead.
Arrow-to-wire conversions the recipe writers share: a schema, a literal and the type tag both carry.
Asked by an executor pricing a call: a probe call that names neither is a call the build side does not have to be there for.
Asked by the recipe writer for what to publish and by an executor for what to build, because answering it twice is how a filtered semi join ended up on the finish path with its residual dropped.
Asserted as the comparison rather than as two numbers: without the doubling the disjoint run compacts as often as the shared one, which is the mutation this catches.
Asserted on the buffer rather than on the recipe, since the recipe names the call and not its payload.
At most two children, so joins are the shape rather than a fan.
At target_partitions>1 DataFusion splits ONE physical file into several byte-RANGE PartitionedFile entries (all the same path), so accept any number of entries as long as they all reference the SAME file; reject genuine multi-file scans (distinct paths, #16).
At target_partitions>1 DataFusion splits one file into several byte-range `PartitionedFile` entries — all the same path — so we key on the distinct path set, not the entry count.
At tp>1 DataFusion splits ONE file into several byte-range entries, all with the same path, so entries are not files.
Backend choice is a turbofish at the entry point, not a selector consulted per node.
Backticks rather than quotes because a name can hold a rendered literal — `Utf8("PROMO%")` — and doubling those is the unreadable half of the problem.
Balancing is by row count and batching is by bytes, and the bytes are the parquet column-chunk totals over the projected columns: a varchar's width is a property of the data, and the file metadata already holds the answer.
Batch forwarding: the nodes that renumber lanes without touching rows.
Batch sizes the estimator derived, one per source in the order translation reaches them.
Batch-sorted is not the rule — sorted on THESE keys is.
Batches each sorted and not sorted against each other is the shape that reads as ordered and is not — it names n rows from wherever the batch boundaries fell.
Batches held and batches released.
Batches packed to the size the estimator solved for this source.
Batches sorted on [a, b] merged on [a] come out ordered by a, which is what the node then declares.
Batches through one accumulator: how many it emitted per arrival, and how many at done.
Batching off still declares MultipleBatches: no downstream phase may assume a lane is one batch, and only an accumulator may make that declaration.
Because it is batch-independent it can be evaluated once per node from the total row count, which is what makes `output_bytes` deterministic at `target_partitions > 1` (the per-batch overhead — bitmap rounding + the offset buffer's `+1` — was the only thing that wobbled with batch boundaries).
Before its build side arrives a join holds only its operators.
Before its build side arrives a join holds only the calls it will make.
Below the scan the same interval does save, and what it saves is reading: the scan stops being scheduled.
Beside the writer rather than beside the renderer, because it is the other half of one claim — a seq means the same node on both sides — and the check that it holds ([`check_seq_kinds`]) is not a rendering concern.
Blocking on the stream is sound for these operators: none of them spawns.
Both are `CudfProject`, and telling them apart in a recipe of five calls is the difference between reading it and decoding it.
Both are early-exit quantities, so both are absent from every node of every run that drained rather than a zero on each of them.
Both are generic over [`Backend`](crate::executor::Backend), so each backend monomorphizes and nothing on the per-batch path is boxed.
Both are plan-time: a shape the engine cannot run is refused where the planner can see it, rather than throwing mid-query.
Both are read twice — by the recipe writer for the wire and by the CPU backend for DataFusion — so a case here is about the rule rather than about either reader.
Both backends emit nothing for an empty lane, which is what makes them one engine here: the device's collapse of no handles is a refusal ([#173](../../../llm-wiki/tickets.md)), so a batch invented on this side would be a row the other cannot produce.
Both branches of `recipe::aggregate` reach the same `finalize_project`, so what this pins is the call list: a second call, per batch rather than at done, since an init that finalizes itself has no compaction to wait for.
Both calls end a lane's build phase, and the hold lifts when every lane has: a lane that owed nothing still has to say so, or the probe subtree is held by a lane that will never build.
Both directions over one batch:
Both engines derive their per-node byte counts from the same declared schema, so a column typed wrongly costs the same on either and no golden byte moves — `avg`'s two state columns were once typed backwards, and only a real divide would have diverged.
Both engines derive their per-node bytes from this same declaration, so it costs the same on either and shows up only in the answer.
Both filter columns pointed at the probe: @0 then reads a valid column of the wrong table, which nothing downstream could detect.
Both get the same share, and what separates their targets afterwards is their own size.
Both halves of `… as name` and the schema entry, which is where a reader resolves an ordinal: unquoted, the name's own space reads as the separator.
Both limit lowerings, asserted on the calls that were and were not made.
Both paths, because they release in different places: a lane call's input goes back where the call failed, a cross-lane one before the result is even looked at.
Both sides call the key `k`, so a lookup by name has two candidates and can only give up.
Both sides carry their key at ordinal 0 here, so a side mix-up changes the text — the case that caught the same slip when the validator went red at T4.
Both sides scatter on their join key, so lane p of one holds exactly what can match lane p of the other; the round-robin repartition below the small side leaves no node at all.
Both tables are tiny, so DataFusion collects the left rather than hashing both sides.
Both the width and the key positions are checked rather than assumed: the keys are taken by position, so a state whose first columns are not the keys would pass the width check and project state columns as keys.
Both the width and the key positions are checked rather than assumed: the keys are taken by position, so a state whose first columns are not the keys would pass the width check and project state columns as keys.
Bounding it by both sides instead would leave the five one-sided types checked against a table wider than the one they have.
Branches disagree by column, so a column is nullable if it is in any branch.
Build an exec executor for every node, whatever its category — a backend wired to the wrong trait, which the driver has to catch where the set was built.
Build columns straight through, plus one typed NULL per probe column the join's projection keeps — what makes the anti join's output the joined schema.
Build one addressed node: take `arity` inputs, stub whatever is missing, number it.
Build the recipe plan for a finished tree.
Build the recipe plan for a finished tree.
By post-order position in the tree, not by seq.
Bytes are the parquet column-chunk totals over the projected columns: a varchar's width is a property of the data, and the file already knows it.
Bytes rather than rows because a narrow table of many rows reads less than a wide table of few; measured on the columns this scan projects, of the row groups pruning left it, so it is a property of the scan and not of the table.
Bytes rather than the batch, so the figure is read once and the same one is released: an arrow batch recomputes its size by walking every array, and two walks are two chances to disagree.
Bytes the build side keeps resident until the join is done.
C++ erased it, and releasing it again is a use of a dead handle.
C++ erases each registry entry as it takes it.
CPU-side memory accounting: how many bytes a batch/schema actually costs.
Calls that reported a measured transient.
Calls with no input batch — `mark_done_and_fetch`, `finish_and_fetch` — are modeled with `n_rows = 0, n_bytes = 0`.
Canonical post-order, the order the estimator indexes by: children left to right, then the node.
Carries a root-adjacent limit's `skip`/`fetch`, because the interval belongs to the crossing: it is a statement about which rows are worth moving over PCIe, and trimming after the transfer ships an unbounded prefix to drop it.
Carry a layout's key distribution and sort order through a projection, keeping only what a bare column reference re-exposes: a projected-away or computed column takes its property with it, and a declaration that outlived its column would be a lie the nodes above it act on.
Caught where the executor is built, not at the first batch.
Checking the map against the tree here is also the only place a routing that names a child or a lane the plan does not have can be caught.
Checks children's schemas, partition topology, key distribution, sortedness and batch layout against this node's requirements, and captured column indices against child schemas.
Child-major: output lane p is lane p of each child, which is why the inputs must share a hash distribution.
Children are snapshotted here because [`GpuNode::children`] builds a `Vec` per call, and the schedule asks for them on every step.
Children first, so this is the position `attach_recipes` gave the same node — the address a recipe is looked up by, and the one the FFI's seqs are numbered in.
Chunks are empty only where there were fewer survivors than lanes: an empty lane is an ordinary shape here, as it is for a hash that lands no key.
Column types in order — the index *is* the ordinal every plan reference uses — plus what those columns mean.
Columns with no group keys and no aggregate state — a scan, a filter, a project.
Compacting on every arrival re-scans the whole state once per batch, which is quadratic where the groups are disjoint and nothing merges; never compacting holds the whole input where the cardinality is high.
Compaction runs on a threshold that doubles when it fails to pay.
Comparing the lowered expression to the original by shape would pin the spelling rather than the meaning — `Sqrt` deliberately comes back as a function call, not a unary — so what is asserted is the array each produces.
Compute surviving parquet row-group indices for a source, so the GPU scan (cuDF `read_parquet` `set_row_groups`) decodes ONLY the groups that can match the scan's static predicate — matching the CPU oracle, which DataFusion's `ParquetExec` already prunes the same way.
Concatenates a lane's batches into one at done.
Construction can refuse — the planner's canonical form is checked there — so this propagates rather than unwrapping, which is what the refusal case asserts on.
Construction lives here rather than on the node so that a node describes what it computes and stops knowing that backends exist.
Consumers that need a node's own parameters — the renderer, a backend's executor match, the serializer — go through [`as_node_ref`] rather than downcasting here.
Contiguity is policy, not a cuDF requirement; changing it regenerates goldens and is treated as that.
Contiguous chunks balanced by row count; within a chunk, consecutive row groups pack greedily while bytes stay under target.
Contiguous ranges over the survivors, each holding about its share of the rows.
Counted per lane every lane would pass its own four rows and the query would return twelve.
Counted rather than netted: a total back at zero is also what releasing something never held would leave.
Counters, not flags: one node can sit in two joins' probe subtrees, and a lift of one of them must not free it from the other.
Counting the group list alone leaves the row a column short, which the width check catches — a refusal where the query has an answer.
Counting the input while the call runs asks for 1600 and cannot fit; pricing the call as if the input were already gone asks for 800 and would.
Counting them counts the groups instead of the rows.
Created and not yet taken as somebody's child, oldest first.
Cross and nested-loop joins carry no type here and owe nothing either: every row they emit is built from a build row, the Left form's padding included.
DataFusion 45's parquet reader emits the Arrow view layouts and comet rejects them; the cast is to the same bytes the device hashes, so the assignment is unchanged.
DataFusion `PhysicalExpr` → the mode's [`Expr`].
DataFusion broadcasts a small build side rather than hashing both, so the sides are not co-located and this mode has no broadcast to do it with (#140).
DataFusion collects its left input, so the build side is already left; what the planner adds is the one batch per lane the join reads it as.
DataFusion declares `avg`'s state as [count, sum] and this mode's decomposition reads [sum, count] (see `decompose`), so a root stopping at the partial emits its state columns the other way round from the schema its own node declares — the one shape where the two differ legitimately.
DataFusion declares avg as [count, sum] and this table reads [sum, count], so a positional pairing types both of them wrongly.
DataFusion declares avg's state as [count, sum] and this mode's decomposition reads [sum, count].
DataFusion emits one where the product itself is what the query asked for — pairing one-row aggregates, as tpcds q9 does — and there is nothing to evaluate per pair.
DataFusion erases the limit node when it can push the bound into the scan, so at tp4 `SELECT * FROM nation LIMIT 3` is a bare scan with limit=3 above nothing.
DataFusion has a third side for a mark join's synthesized column, and a filter reading it would otherwise be silently rebased onto the probe.
DataFusion has no entry point for "run this operator on these rows": a node executes against its children's streams.
DataFusion interleaves where its branches share a hash, so lane p of one belongs beside lane p of the next.
DataFusion may answer a batch with several or with none (a filter that kept nothing emits nothing), so the pieces are concatenated and an empty answer becomes an empty batch of the node's schema rather than a missing one.
DataFusion merges adjacent limits, so the nested form only survives with something between them: a limited aggregate subquery under a cross join, with a limit at the root, arrives as an interval on the GpuUnload and another on a GpuLimit below it.
DataFusion names an aggregate output by its own expression text, and tpcds aliases like `order count`.
DataFusion never emits this shape, so it is built by hand — and the point is where the refusal comes from:
DataFusion parks a CoalesceBatchesExec above the filter at tp4, so the node is found rather than assumed.
DataFusion physical plan → the mode's node tree.
DataFusion spells a shuffle as a hash repartition and a lane collapse as a coalesce, and which one it chose is what decides whether this aggregate re-lands its rows by key or merges them into one lane.
DataFusion uses a nested-loop join with no predicate where the product itself is what the query asked for — tpcds q9 pairs one-row aggregates that way — and that is a cross join by any other name.
DataFusion's `AggregateExec` in Partial mode probes its own aggregation ratio after 100,000 rows and, where the groups are nearly as many as the rows, stops grouping and passes its input through as state — which is sound only because a Final stage regroups downstream.
DataFusion's `ParquetExec` carries paths, and the plan text and every node above it name the table.
DataFusion's expression in, this mode's in the middle, DataFusion's back out, and the same rows at both ends.
DataFusion's expression, this mode's, and DataFusion's again must all read the same column out of the same rows.
DataFusion's file groups are byte ranges rather than files, so several of them carry one path; a row-group index means nothing without the file it indexes, and the node reads its own from here.
DataFusion's filter drops columns as well as rows.
DataFusion's filter projects as well as filtering, and dropping that would leave the node declaring its child's columns while emitting fewer.
DataFusion's filter projects as well as filtering, and the wire format carries the same pair.
DataFusion's join type, restricted to what a nested-loop join can run: the C++ rejects anything else outright.
DataFusion's limit pushdown parks a limit here, and dropping the node with it would answer a count over three rows with the count of all of them.
DataFusion's limit pushdown parks this one on a CoalesceBatchesExec as a fetch, so a translation that merely drops that node counts every row.
DataFusion's own Welford state types, which is what makes the finalize's typing a question at all: the count is unsigned and the output it feeds is a float.
DataFusion's own stddev accumulator, reading its input as state rather than as values.
DataFusion's partial aggregate stops grouping where grouping is not paying, which is sound under a Final stage and wrong here: the merge is a Partial too, so duplicate keys would reach the finalize as extra rows.
DataFusion's physical plan in, a node tree and its memory model out.
DataFusion's projection as this layer numbers ordinals.
Decided by the node's type and not by the calls it published: a Left outer's finish is a LeftAnti too, and its answer is those build rows PADDED — so a switch reading the kind at done would hand a Left outer the wrong rows rather than refusing it.
Deciding and doing are separate on purpose.
Decimal precision and scale stay: an explicit cast's target is unreadable without them.
Decimals come first among the literals because that is where a wrong write is invisible: a precision or a scale dropped here reads as a plausible number on a device and as nothing at all in a plan.
Declarations only — nothing here executes, and the vocabulary is fixed by `llm-wiki/architecture.md`.
Declared nullability says nothing — every column in both benchmarks is declared nullable, primary keys included — so this is the statistic instead, and an absent count reads as "yes" rather than "no".
Degrades SAFELY otherwise (open fails → None).
Deleting the call from the planner turns this red and nothing else.
Deliberately not DataFusion's `RepartitionExec`, whose ahash lands the same key in a different partition.
Deliberately not [`RecipePlan::nodes`] and deliberately named apart: the two count different trees and mostly disagree — a node with several calls, a stub, a union each separate them — so a driver that checked the wrong one against the C++ would be comparing two true numbers about two different things.
Deliberately-tiny budget used to force many small batches in the batch-sizing stress tests.
Dense `index` for the cache; `node` and `lane` ride along so a diagnostic can name the site with no formatting per call.
Derived rather than transcribed: restating `can_step`'s disjunction here would keep this green through any change made in both places.
Deriving the type a computed column would actually have — a project's expression, an aggregate's state — and checking the declaration against it is [#163](../../../llm-wiki/tickets.md).
Dictionary: count the keys deterministically (rows × key width).
Drive to the failure, and report what the driver had recorded when it stopped — which is how a refused call is told from one that ran and failed.
Driven by hand and abandoned, which is the only way to reach the condition: the driver's own schedule does not leave work behind.
Dropping it leaves the node declaring the projected columns and emitting all of them, so every ordinal above it reads one column of some other one (#135).
Dropping it would leave this node declaring its child's columns while emitting fewer, and every ordinal above it reading the wrong one.
Dropping rows leaves every declared property standing — the lanes, the order within a batch, the key each lane holds — but dropping columns rebases them.
Dropping that is what makes a correct plan fail the co-location guard above an aggregate.
Each batch sorted as it arrives, the runs merged into one at done — and nothing at all where none arrived, since a merge of no runs is the collapse of nothing by another name and the device refuses that (#173).
Each counts the stream its own node is handed, which is what nesting means.
Each is handed its plan node, the schemas that node declares it consumes, and the child offsets the walk chose for its structural slots — so a payload is written from the node alone, as the mapping table is a per-node claim.
Each named input is one child slot — the C++ reads its output count off the first slot, so two handles in one would ask for two outputs and be refused for a buffer it never needed.
Each node's children-first position, in the driver's own pre-order.
Each owes the same two matched rows plus whatever its preserved side adds — and for Left and Full those extra rows can only be known once every probe batch has gone past, which is what the finish pass is.
Each part is emitted only where this lane count and batch layout need it — a one-lane region never splits, so there is nothing to merge back.
Each side's key is an ordinal into that side, so each is read against that side's schema.
Each stage holds at most n rows per live batch instead of its whole input.
Either a call or a named violation — never a silence the driver would have to invent an answer for.
Emits each batch as it arrives and holds nothing — what a mid-plan limit does.
Emits this many batches at done, holding nothing.
Empty for Full, which pads both.
Empty on the first pass, when there is nothing derived yet.
Empty scatter outputs are dropped here, so nothing empty traverses a chain because of hash skew.
Empty unless this node expands grouping sets, which only an init node does: it emits `__grouping_id` as an ordinary column and every node above groups on the keys plus that column.
Empty, not merely bounded: nothing would drain a probe batch yet.
Enough rows have passed this node that no later one can change its answer.
Enough rows have reached this node that no later one can change its answer.
Equal at the end of every run, on both the drained path and the early-exit one.
Equal shares rather than proportional ones: a proportional split hands the most budget to the source already producing the most bytes.
Every arm of the IR enum appears above: a new operator added to one side and not the other is what this count catches.
Every arm reads its own node and the schemas above, and nothing else.
Every assertion in this directory is measured against this backend, so a mock that miscounts is a thousand lines of tests agreeing with the wrong answer and staying green.
Every batch one lane's source produces, as the rows and bytes it reported.
Every batch the lane produced, as the values in it, so a case reads as the rows the mapping named.
Every batch this lane accumulated.
Every branch below is about the batch this call consumes, and the calls that end a lane consume none: an interval decides nothing about them.
Every budget here is derived from the peak an unbudgeted run of the same plan reported, never written down: a byte count in a test is a magic number that rots the first time a mock's sizes move, and it can make a test pass for a reason nobody meant.
Every build, as the node's address and the post-order it was handed.
Every cardinality here is the trivial estimate the planner has today (#19, #73): a filter passes everything and a join is 1:1.
Every case asserts on calls — pull counts, queue bounds, batch release, the trace — rather than on the rows that came back: a limit test that checks the rows passes just as well when the whole input was read and thrown away.
Every case here is a unit case: the accountant takes figures, so nothing below builds a plan or a backend.
Every cell of both dimensions — nine join types crossed with a residual filter — and one claim about each: the path this writer publishes and the path the CPU executor builds are the same path.
Every column reference must be in range of the schema it reads AND carry the name of the field at that position.
Every consumer of those parameters goes through `as_node_ref` instead.
Every declaration the component offers is here.
Every declaration the component offers is here; the implementation modules hold the trait impls, the constructors' bodies and the checks.
Every entry in the mapping came from these survivors, so a miss would be a mapping addressing a row group this scan does not read.
Every executor here runs the recipe its node published, so the whole of building one is finding that recipe — which is what the context carries beside the session pointer, and why `executors_for` takes a post-order rather than deriving one.
Every executor models a share of the batch it is about to be handed, and nothing else.
Every executor was written to the trait's shape before a `Backend` existed to check it, so this file is where the compiler reads them.
Every expression before every name, which is the order the payload golden was canonized in: a builder writes back to front, so interleaving the two would move bytes that mean the same thing.
Every key distinct, so nothing ever merges and the state grows by `ROWS` each time.
Every lane would honour it, so N lanes would return N times the rows; one lane is what makes the loader's own limit the answer.
Every lane's handle, partition-major.
Every method that ends a protocol consumes `self`, which makes four run-time guards the prototype needed into compile errors: probing before `set_build`, a second `set_build`, probing after `finish_and_fetch`, and accumulating after `mark_done_and_fetch`.
Every node kind is asked here instead.
Every node kind, as a borrow of the concrete node.
Every node the predicate accepts, deepest first — the order the aggregate sequence runs in, so `[init, per-lane merge, finalizing merge]` reads as written.
Every node's post-order address, indexed by its pre-order one — the numbering the recipes and the FFI share.
Every node's post-order address, indexed by its pre-order one.
Every node's recipe, indexed by the post-order position the estimator and the memory golden already number by, plus the bytes those recipes address.
Every one can fail, and a failure ends the query: the C++ side resets the session and every resident table with it, so there is nothing to resume from.
Every one of these is read far from where it is computed — a subtree range decides which nodes a join holds, `ready_lanes` decides when a cross-lane node is runnable, and `slot_base` decides whose residency a call is charged to — so a wrong one shows as a deadlock or a mis-billed byte rather than as a wrong index.
Every operand in the output's type, the count cast up to it before anything is subtracted.
Every operand in the output's type.
Every other finishing type still emits this batch's matches, and the join below consumes the batch — hence the copy the keys come off.
Every other join case in this file passes `projection:
Every other node this backend drives takes the one-output form above.
Every other way out of a `GpuBatch` runs `Drop`.
Every published seq holds a node of the kind its recipe claims.
Every published seq resolves to a node, and to a node of the kind its recipe claims.
Every reference in a join filter is an ordinal into the filter's own table, so each one has to name a mapped column AND find its own name on the side that column comes from — a mapping that points at the wrong side otherwise reads a valid column of the wrong table, which nothing downstream can detect.
Every row into one lane, the rest emitted empty — hash skew at its worst.
Every row the join emitted, rendered and sorted: a join's output order is its probe batches' order and its finish pass comes last, so what a mode owes is a multiset.
Every row under one key, so every compaction leaves the state a single row.
Every row, which is what a node with no interval above it asks for.
Every run that finished has to have released what it held, and a peak of zero means the accountant watched nothing.
Every state batch here is written down rather than produced by an init aggregate — a merge that reads state has to be provable without the node below it — and every expected answer is hand-computed.
Every state transition emits in the same call, so there is no wrong interleaving to construct and output timing is a pure function of the call sequence.
Every subtree a shape declares is contiguous, which is what the hold ranges are.
Every table here is small, and what these tests need is the four-lane sequence.
Every time the accumulation compacts, and once more at done.
Every unpadded side must carry the same fact, or the two disagree about where the rows are and neither claim describes the output.
Every variant writes, `Sqrt` included: the schema gained it so that a finalizing aggregate carries its own finalize rather than leaving the arithmetic to the C++.
Every variant, every operator, and the literals the corpus actually produces.
Everything above the aggregate's merge is served by what the merge emits, so a project up there cannot change what the source may spend.
Everything but the Welford triple is one of ours to one SQL name; the triple folds into the `stddev`/`var` it decomposes, at the position of the first of its three, because neither engine has an `m2` of its own.
Everything cross-partition is the partitioned driver's.
Everything else is a function of the schema, so the two engines charge a row the same bytes without either reading the other's arrays.
Everything else is relabelled, and `try_new` refuses what cannot be.
Everything else plans: semi honours the flag, and `null_equals_null=true` is asking for the equality the executor hardcodes.
Everything that knows what a flat buffer looks like: the recipe vocabulary, the writers that build the plan the C++ is handed, the reader, and the two renderers.
Everything the aggregators and the finalize expressions produce: a sum over no rows is NULL, and a stddev under its ddof is NULL by construction.
Exact rather than coercible — a state column arriving as another type is a decomposition that has gone wrong upstream, and coercing it would merge the wrong numbers rather than say so.
Exactly N batches, in lane order, empty where the hash sent nothing.
Exactly N outputs, some of them empty; N is a plan value, so the count is checked once inside the returned type rather than at each call site.
Execute exactly one plan node, fed by its children's already-computed batches in child order.
Exhaustion consumed the source, so the slot's liveness is the state itself.
Exhaustion consumes the source, so the driver's slot IS its liveness.
Exhaustive on purpose: a dispersion output that is not a float is a decomposition that has changed, and picking a default here would move the arithmetic rather than say so.
Exhaustive rather than defaulted: a side the output does not hold has no ordinal, and a join type reaching a catch-all would silently lose a claim it earned.
Exhaustive:
Expect rather than default: a seq with no node is the numbering outrunning the tree, and printing nothing there would render a plan whose every later seq addresses the wrong node as if it were fine.
Expected — a join's model rests on a cardinality estimate — so it is recorded with its magnitude rather than asserted away.
Exposed for one guard: that these are the positions `attach_recipes` gave the same nodes.
Fails the query, and says which node and lane it was calling.
Faithful port of datafusion-44 `row_group_filter::RowGroupPruningStatistics`: adapts parquet row-group column-chunk statistics to the `PruningStatistics` the `PruningPredicate` consumes.
False for the build-side semi family, whose probe call is the key project alone — and the accounting has to know, because a transient charged for a read that never happens refuses work that fits.
False for the build-side semi family, whose probe call is the key project alone — and the accounting has to know, because a transient charged for a read that never happens refuses work that fits.
False for the three types that preserve unmatched PROBE rows: what they owe is the probe side, padded or not, and making it takes a call over a build table that does not exist.
False for the three types that preserve unmatched PROBE rows: what they owe is the probe side, padded or not, and making it takes a call over a build table that does not exist.
False for the two semi families and for a mark join, whose row is one side's — read by an executor deciding whether the columns it owes have to be invented or only selected.
False for the two semi families and for a mark join, whose row is one side's — read by an executor deciding whether the columns it owes have to be invented or only selected.
Finish on the one offset left, which is the root by construction: the last node created is the last visited in post-order.
Fixed-width types contribute 0.
Flat columns only.
Flow, backpressure, limits and accounting, over the mock backend.
Fold every pending arrival into the state, and raise the bar.
Forwarders are wired once:
Four lanes each honouring it would answer with twelve rows.
Four lanes for the unload and the join, which take the emitter's; one for the coalesce; one apiece for the merge and the emitter, which are cross-lane; and two for the sort and its source, which are the build side's.
Four lanes hold four batches at once, so each may be a quarter the size.
Four lanes over two row groups: the mapping says two of them read nothing rather than inventing work, so this is the shape the goldens carry and not a defect.
Four of ours map by name; the Welford triple does not, since no `m2` name exists there.
From DataFusion, per join:
From the sf1 measurement at full projection: the largest table that must stay on one lane is tpcds date_dim at 4,006,445 bytes, the smallest that must not is tpcds web_returns at 8,041,397, and tpch supplier at 1,532,237 sets the floor. 5 MiB sits in that gap nearer the lower end, so date_dim would have to grow 31% to cross it and web_returns shrink 35%.
Full outer reaches the goldens nowhere — no corpus query plans one — so this is the only place its five calls are checked at all.
Full pads both ways: an unmatched build row reads NULL where the probe key was and an unmatched probe row reads NULL where the build key was, so neither column holds the value that placed every row.
Genuinely several files would each have their own row groups, and this mapping addresses one file's — measuring the first and sizing the whole plan from it would be a wrong answer rather than an error.
Going back is not free: a column reference is an ordinal into a child whose column order this mode decided, so the name that rides beside it is checked against the schema at that position rather than trusted — the mismatch this catches is the one a rebase gets wrong, and it is silent on the device.
GpuMergePartitions, GpuUnion, GpuInterleave — routing only, no backend.
Group keys, then what the node emits per aggregate: its state columns where it hands state on, and one finalized column each where it finishes.
Grouped by the child slot each fills, not flattened: the C++ reads its output count off child 0's, so a join's two handles in one group would ask for two outputs and be refused for a buffer it never needed.
Grouping re-keys the rows, so no input order survives — but a hash on columns the group list re-emits does: those rows are still where the hash put them, at the ordinals the keys now occupy.
Grouping sets add one output column, `__grouping_id`, which the init emits like any other and everything above groups on beside the keys.
Grown by expansion rather than by attaching to an arbitrary earlier node: only that gives the pre-order numbering `PlanIndex` produces, and the contiguous subtrees every hold range rests on.
Growth stops when a node draws no children, so the tree is at most this big and every index below comes off what it actually produced.
HARD fail on any other unhandled type: the old silent 0 undercounted decimals/Utf8View, and an allocation-based fallback (get_array_memory_size) would make goldens non-deterministic.
Hand the handle to an FFI call that consumes it — a slice, or an executor call taking it as an input.
Hand the handle to an FFI call that consumes it.
Hand-built because the sql that reaches this branch also hits #166: a fetch-carrying CoalesceBatchesExec keeps its input's lanes and lowers here too, but only ever with skip=0, and the query that pinned that shape was reshaped away.
Handles thread from one call to the next.
Hash keys are ordinals into the node's input, so anything else is a shape this mode does not plan rather than something to evaluate on the way to the hash.
Hashed on a column it does not group by, so a group's rows are spread across lanes and each lane would answer for part of it.
Hashed on a column that is not the join key: lane p of one side then holds rows whose matches are in some other lane of the other, and each lane answers for part of a key it cannot see.
Held and released, counted rather than netted: a total back at zero is also what a release of something never held would leave behind.
Held by `GpuAggregate::intermediate`'s documented order today, and by this if that ever moves.
Held bytes, which every accumulator answers and only the aggregate tracks incrementally.
Held bytes, which every accumulator answers off the handles it is keeping.
Hence one walk that builds the buffer and numbers as it goes.
Here each event that can change an answer updates the one node it touches, and a pick is a scan for the lowest set bit of a bitset ordered by (height, order).
Holding is the whole difference from an exec node.
Holds every batch and emits one at done, its residency growing as it holds.
How a lane's row groups are cut into batches.
How a refusal prints.
How a state merges.
How deep the recipe plan is.
How deep the serialized plan is, which the C++ verifier bounds.
How far under: 2.0 means the call used twice what was modelled.
How many calls reported a measured transient rather than `None`.
How many columns a join emits before any projection of its own — the count half of [`emits`], which is the same fact its carry-over rule reads.
How many columns a join emits before any projection of its own — the count half of [`emits`], which is the same fact its carry-over rule reads.
How many executor sets have been built.
How many independently-ready units the schedule tracks for this node.
How many lanes call, which is the multiplier on every `per batch` below it: one per lane where the executor is lane-scoped, and exactly one at the cross-lane points.
How many nodes are unconsumed — a caller's mark for [`Writer::reduce`].
How many rows have gone past, which is what a driver reads to know the interval is satisfied and the pulls can stop.
How many sources this translator reached, which is what pairs a pass with the one that sized it: the two address a source by nothing but that order.
How many times the threshold was reached.
How many typed NULLs the pad project appends: the probe columns the join's projection keeps.
How rows were routed into lanes.
How the two run-time errors print, and what a `RunError` becomes at the DataFusion boundary.
Idempotent, so a step that ends a lane without calling anything can say so too.
Idempotent, so the driver may recompute a node's lanes after any step without tracking what changed.
If bloom-filter parquet is ever introduced, the CPU oracle would prune MORE (equality predicates); our stats-only survivors would then be a safe SUPERSET — the GPU reads a few extra groups and `GpuFilterExec` drops their rows, so NO data loss, but the GPU scan row count could exceed the `.cpu.txt` oracle.
In this mode nothing does: the init emits state and the merge is a Partial too, so a skipped grouping reaches the finalize as duplicate keys and comes out as extra rows.
Indexed by the child's lane rather than the consumer's, so it lines up with that child's own [`emitted`](RunReport::emitted) where the two differ — an emitter redistributes, so nothing else would sum.
Inner streams; Left takes a single-batch probe, since the finish trick accumulates keys and a predicate join has none.
Inner, Right and the probe-side semi family: every emitted row is decided by (build, this batch), so nothing accumulates and there is no finish.
Interleaving lanes keeps neither the hash that separated them nor an order that held within one of them.
Intervals nest — each counts the stream its own node is handed — and the spec's limit lowering rule says why only the non-adjacent form ever arrives that way.
It accumulates nothing and makes no backend call — the driver owns the rotation.
It answers the same question as `select` and is checked against it over the whole cross product in the tests, because two readings of one rule are two rules.
It becomes a real third state only under #138's ranged merge emission, which orders a stream across several batches.
It bites only while batching is on, which is what makes the threshold mean anything.
It carries no key, so it says nothing this mode acts on: lanes come from the partitioner's mapping instead.
It covers the nodes that CARRY a column.
It crosses the boundary as data because a caller may one day answer the two differently — a pre-call refusal is a call that never ran, where a post-call one is work already done.
It emits everything it merged, once, at done.
It has no default here: the number comes from the same budget rule that sizes loader batches, which is the driver's (T17), and a stand-in default would be a policy nobody chose.
It holds its whole subtree, itself included, and the hold never lifts — so this is where a run ends with lanes not done and queues non-empty.
It is a backstop against a lowering that emits ranges before the accumulator has them all (#138).
It is a slice of what the sort ordered rather than a top-N heap, so the rows kept are the ones whose keys win and the same run answers the same way twice — which is what an oracle owes, since neither engine's sort promises a tie order.
It is also the output schema where there is no finalize.
It is held rather than consumed: which call takes it, and whether it survives that call, is what the recipe says.
It is marked done as it is held, or the hold would stop it reporting and strand its parent — `LIMIT 0` is the case that forces it.
It is not universal — contiguity is the stronger rule, so row groups differing by orders of magnitude inside one file can beat it.
It is rebuilt into the one SQL aggregate it decomposes, which is what the schema's `agg_state` annotations are for: they say which output columns belong to which aggregate, and with what `ddof`.
It is sent a plan in the legacy vocabulary whose nodes exist to be addressed — a menu of parameterized kernels — and the driver then calls the ABI as often as its own schedule wants.
It is the gap `AggregateMode::Merge` was added to the wire to fill.
It is the pad that makes this worth a case rather than the join: the anti join at done emits every build column whatever the projection says, so the project above it is what cuts the row down, and it walks the projection rather than the build side.
It is what stops a join's hold from covering nodes outside its probe.
It keeps the `(2, 20)` pair and drops `(2, 21)`.
It prints only where a run left something behind.
It reads the projected bytes of the surviving row groups, so a narrow scan of a big table falls below it — the rule working, not a value to retune.
It rides in from `PlanIndex`, which computes it in the walk it already makes: the schedule needs pre-order and the recipes are addressed post-order, and a backend deriving the second from the first would be a third walk that agrees with the other two by coincidence.
It runs after planning because a recipe is a statement about a node, and a node is not finished until the plan is.
It runs after planning because a recipe is a statement about a node, and a node is not finished until the plan is.
It selects columns by NAME (`scan.cpp` builds `projected_names` and hands them to cuDF), so this reads exactly the columns the node declares — where naming the file's own ordinals would need the file's full column list, which a plan node does not carry.
It streams and holds nothing — a batch outside the interval is released uncalled, one inside is forwarded untouched, and only the two straddling its ends are sliced.
It takes one call per lane event because that is what round-robin driving produces, and the call carrying the last `Done` is the emitting one.
It takes the model to reach it — see the two tests above for why.
Its absence was two engines answering with different columns.
Its finish pass accumulates keys and a predicate join has none, so the probe side has to be whole before the join runs.
Its input ended, and an exec has nothing to emit at the end — so the lane ends with no call at all rather than a call that produces nothing.
Its input is NOT required to be one batch: requiring that would read the whole of a subquery's table to answer for a hundred rows.
Its input is one lane — the node checks that — so the count it keeps is the count of the stream.
Its input is one lane — the node checks that — so the count it keeps is the stream's.
Its name and type are DataFusion's, off the partial's own schema.
Its output is the build side plus the boolean, so the build key is still at its own ordinal — a mark join treated as emitting neither side loses a claim it earned, and every co-partitioned node above it then collapses to one lane.
Its own predicate, so the filter above the limit reads the same schema the limit hands it.
Its presence is the only thing that distinguishes a merging node from a finalizing one.
Its state is gone, so it stops contributing and there is nothing left to ask for a figure.
Its tests are the driver's, in `driver/tests/render.rs`: rendering a report takes a run, and the mock backend that makes one deterministic lives there.
Its three columns merge in one call: the merged mean needs the counts and the cross term, so summing each column on its own is a different answer.
Its unmatched build rows carry NULL in the probe key, so the build column is the one that holds the placement value for every output row.
Kept beside the variant so the field and the sentence cannot drift: the field is for code and the sentence is for a person, and neither replaces the other.
Kept identical so GPU pruning == CPU pruning.
Kept so a caller passing an output-lane index where a readiness index belongs is caught here rather than silently writing into the next node's flags.
Keys plus state where it hands state on.
Keys plus the id, so a rollup row is merged with its own set and not another's.
LIMIT n)`: an accumulator above the limit only emits when it is told its input ended, and satisfaction is what ends it — no later batch is coming.
Label component of a device string (`tp1` / `tp8`).
Label component of a device string.
Lane 0 has nothing, so its exec goes straight to EndOfInput and lane 1 makes the first call.
Lane 1 has nothing at all, which is what a scan whose lane got no row group looks like: it is simply never runnable, and needs no mechanism of its own.
Lane count and batch layout always; a hash or an order only where one is declared, since a line saying a node declares nothing says nothing.
Lane count and batch layout on every node; a hash or an order only where one is declared.
Lane counts sum, and no branch's hash survives the renumbering.
Lane counts sum: output lane k is served by exactly one (child, lane).
Lane p holds every branch's lane p, so each batch is still whatever it was — an order within a batch survives — but k branches make k batches out of one lane.
Lane p is lane p of every branch, so a branch hashed on another key would put rows that cannot meet into the same lane.
Lane p of one side holds exactly the rows that can match lane p of the other.
Lane p of one side holds what can match lane p of the other only if both were scattered on their own join keys, in key order — equal lane counts are not the same fact, and two four-lane scans agree on nothing.
Lane-scoped work is delegated to [`super::single_partition`]; the three cross-lane categories are here, along with the one node the driver special-cases, a `GpuUnload` carrying a limit.
Lanes a source is partitioned into, before the small-table rule.
Lanes outermost and batches within, which is `partition_groups`' own nesting: on a source, element i of lane j is one batch in all three lists and in the mapping.
Lanes outermost and batches within, which is what makes element `i` of lane `j` one batch in all three lists — and `in_rows` indexed by the child's lane, which on an emitter is the only indexing that can be compared to anything.
Large enough that arrow's own accounting separates a state of one row from a state of thousands, which is the difference the threshold reads.
Layout and schema live inside the kind.
Leave one offset for everything created since `mark`: what the node produced if that is all there is, or — the third rule — a structural union over the branches it did not consume, because an unreachable node is never indexed and every seq above it would shift with nothing saying why.
Left and Full ask which build rows nothing ever matched; the semi family asks its own question, and asks it with the node's own NULL semantics, so the pass substitutes for a legacy single call rather than improving on it (#59, #80).
Left and Full ask which build rows nothing ever matched; the semi family asks its own question, and asks it with the node's own NULL semantics, so the pass substitutes for a legacy single call rather than improving on it (#59, #80).
Left as its filter-schema ordinal only where the map is short, which validation refuses.
Left emits this batch's matches as an Inner; Full also emits the probe rows this batch had no match for, which is batch-local because the build side is complete.
Left keeps its build rows and pads the probe.
Legacy lowers the same shape in its wrapper rule.
Logical `output_bytes` for a node from its output schema, total row count, and the Σ var-length CONTENT bytes — the data-dependent term, which is what a device measures and a batch of Arrow arrays is read for.
Long enough to leave the insertion sort behind.
Look through the nodes a shuffle is spelled with.
Lowering has to find the session's own `sqrt` rather than compute a second one, so this asserts the digits and not the shape.
Maintained incrementally rather than rescanned.
Make exactly one call.
Masks are as long as the group list by construction, so this is the default and not a shape: a missing entry has to read as excluding, or a malformed body would buy a co-location claim the rows do not have.
May consult `self`, so an accumulator includes its state.
Measured here, one four-row batch per lane under `LIMIT 5`: lane 0: source emitted [4], merge consumed 4 lane 1: source emitted [4], merge consumed 4 lane 2: source emitted [4], merge consumed 0 <- the shortfall merge emitted [4, 4], unload consumed 8 <- the limit's own node, at equality
Measured in pulls rather than in bytes, because a 1:1 chain peaks at one batch however much of it is read — the peak is the wrong instrument here and the read count is the right one.
Mid-plan, the interval is a real node over a one-lane stream.
Minting one instead, from the keys and the output names alone, declares a hash over lanes nothing ever scattered.
Moot today: our DuckDB `COPY ... (FORMAT parquet)` files carry no bloom filters, so the CPU oracle doesn't bloom-prune either => the stats-only survivor set is EXACTLY the oracle's.
Multi-file, no predicate, or join/dynamic (date_dim) ranges => `None` (read all groups, as today; #16).
Must NOT use get_array_memory_size here — that's allocation-dependent (buffer capacity) and varies run-to-run, making the goldens non-deterministic.
Must match the GPU kernel's exactly — negative hashes wrap the same way — or per-partition row counts diverge from the golden.
N -> 1: lane 0 takes every lane of the one child, round-robin.
N is a plan value, so the driver checks it.
N lanes into 1, forwarding each batch as it is visited, round-robin.
N lanes of one batch each leave one lane holding N of them.
N lanes of sorted batches into one sorted batch: every k·m batch goes into one merge at done, and the `fetch` is applied to the result.
N lanes, nothing else declared — what a scan or a shuffle-free chain emits.
NOT a [`MemoryLimit`] tier — no device is ever configured this low; it only exists to push the batch-size math to its floor.
Named for the call rather than for the node, since `GpuUnload` is the node.
Names as well as types: the declared names are what every `name@ordinal` check above this node resolves against, so a branch naming its columns differently makes one of the two readings wrong wherever they are compared.
Names the fix, since the node knows it: "the planner inserts `GpuMergePartitions` below it".
Names the shape, not the node's fix.
Neither carries a phase: each declares aggregators over its own input and, where it finishes the aggregate, how each aggregate finishes.
Neither has a join type of its own on the wire, and neither publishes a finish, so there is no answer for one over no keys to be wrong about.
Neither implementation is `Clone`: a `GpuBatch` cannot be, and the free `RecordBatch` clone is not worth the asymmetry (a future dual consumer writes an explicit copy, #140).
Neither touches a row — the branch type normalization the union kernel does inside the executor is a per-branch `GpuProject` the planner inserts, which is what leaves these two as pure routing.
Nested types are NOT handled here — `ColAccum` computes them from per-level totals (List child overhead can't be derived from the parent row count alone).
Never `Final`, which would also finalize, and a finalize here is a project.
Never a mean of means: a sum and a count, each merged by sum, divided at the end.
No ELSE is an implicit NULL, and every branch is a value the CASE can return.
No `Exec` suffix: these are not DataFusion nodes, and after the wire-format rename a line from either family says which mode produced it without a caption.
No backends and no `CallStats` — routing is driver work, and a batch's bytes are already accounted as driver-held in flight.
No batch can arrive again, and this category has nothing to emit at the end — the lane simply ends, with no call made.
No call was made and none will be: what the lane owed was nothing.
No call was made, which is the whole saving and the thing a test on the rows returned cannot see.
No key to co-locate on, so both sides are one lane and every build row meets every row of the batch.
No literal in it: the build-side semi family's finish already emits the row, and a projection only narrows it.
No part of Welford is written out here, because the device runs cuDF's MERGE_M2 and two spellings differ in the last digits.
No plan reaches it today: every `SortExec` lowers to a per-batch sort under an accumulator, so an ordered stream is the only ordered thing there is.
No recipe is read here: this backend asks DataFusion for the operator a node describes, and the recipe is what the other one needs to address a seq.
No residual and no projection — a build-preserving type asks which build rows matched, and the pad project is what makes the output the joined schema.
No seq at all: a limit's bounds are runtime values, so it addresses the slice symbol rather than a node.
No seq: skip and fetch are frozen per node, and the right bound for a batch is a runtime value no frozen node can carry.
No shape this mode plans sends a name `is_avg` matches: inside the Welford triple the func is named by `sql_name`, and `wire_name`'s `"mean"` is reachable only from the arm below it, which decomposition leaves no `Mean` aggregator to enter.
No special operator: the init emits __grouping_id as an ordinary column, and every node above groups on the keys plus that column. #65 is about the id's ENCODING, which is execution-side and does not touch the shape pinned here.
No statistic is not a promise of no nulls.
Node indices are pre-order, so a subtree is a contiguous range and "order" — the leftmost tie-break — is the index itself.
Node indices are pre-order, which buys two things the driver leans on: a subtree is a contiguous range, and "order" — the leftmost tie-break — is the index itself.
Node numbering is pre-order, so the root is 0 and a chain descends from it.
Node numbering is pre-order: 0 unload, 1 join, 2 coalesce, 3 build source, 4 filter, 5 probe source.
Nodes 1 and 3 share a height, and the build-side-drains-first argument is exactly this tie going to 1.
Nodes in the PLAN TREE — this mode's own, the length the memory model's per-node vector has, and what a consumer checks its own tree against before reading a `None` as an answer.
Nodes in the RECIPE PLAN — the fb tree, stubs and structural unions included, which is what `peacock_executor_begin_plan` reports through `out_node_count`.
None is "this run was not instrumented", which is not a model of zero.
None`, so nothing here reached that walk — which is the CPU twin of a defect the device tests already pin.
Nor are the state column TYPES, which no rule anywhere derives from what produces them — [#163](../../../llm-wiki/tickets.md).
Not `Clone`, deliberately — symmetry with `GpuBatch`, whose handle cannot be.
Not a handle either: an offset and a length counted at run time.
Not a handle: the row groups this batch reads, overriding the node's own list.
Not a limitation of this mode: the shipping executor applies the filter after the outer gather, so a padded row's NULLs drop it (#153).
Not a shape this walk can describe; the node arm will refuse it.
Not finished: the probe side is still producing for this lane, and what it produces has to be read to be let go of.
Not one row is wanted, so it never crosses the boundary: the batch is released where it stands, and the saving on the skip prefix is unbounded.
Not reachable from today's planner: where a limit is root-adjacent DataFusion leaves a GlobalLimitExec there and uses a coalesce's fetch only as the bound pushed below it.
Nothing addresses a stub or a structural union: they hold the tree together, and the seqs a recipe publishes point at the nodes that carry a call.
Nothing at done: a cross join's answer is decided by (build, this batch).
Nothing co-locates them, and this mode has no broadcast to do it with.
Nothing downstream can see that, which is why it is refused here.
Nothing downstream may depend on the shape batches arrive in.
Nothing here builds a cuDF call of its own, deliberately: the recipe already says which seqs to address and in what order, so an executor that also built calls would be a second path to the same kernels, and the two would drift where the plan golden pins only the recipe.
Nothing here knows about backends, batches or executors: it takes events and returns a node.
Nothing is modelled, so every pre-check passes; the accumulator keeps what it is handed, and the check after the call is what notices.
Nothing is substituted: a placeholder would be a node the recipe names as one kind and the buffer holds as another, and running it would return an empty or concatenated table rather than an error.
Nothing lifts a limit's hold, so the check that a second satisfy did not double the counter is that the node is held by exactly one: drop it once and it is free.
Nothing pruned -> behave exactly as today (empty list = cuDF reads all groups).
Nothing reaches it: this mode finalizes in a project, so the aggregate is only ever asked for its state.
Nothing reads a group-key annotation through a projection, and nothing claims one either: only the aggregate nodes declare them.
Nothing: concatenating a lane's batches requires nothing of them — any lane count, any batch count, any order — and it carries no parameter of its own to be wrong.
Nothing: it forwards each batch it is handed, so every lane count and batch layout is one it can take — and what it declares of its own output, the claims it drops, is checked by the structural pass.
Off the erased value rather than the node, so a node can reach the registry from a default trait method — where `Self` is not yet known to be sized.
Off the registry rather than beside it: the node set and the category set are one mapping, and a second source of truth for it is a second thing to keep true.
On a single worker that is a deadlock; this is the shape a driver has to leave room for, and the header that says so was on no test's path until now.
On the line with the calls and nowhere else — a node that makes none has no number to give.
Once per PROCESS and not per call: libtest runs these cases in parallel, and a check-then-create is true the moment the file is created — before its footer is written — so the second thread would read a parquet with no footer.
Once per handle that reaches the sink.
Once, when the node's input is complete.
One 800-byte batch under a budget of 1000, modelling a byte of scratch per input byte.
One ABI call: the symbol, the seq it addresses where it takes one, and what it passes.
One DataFusion node over batches that are already in hand.
One DataFusion node over the batches it is handed, which is the whole of what a CPU executor does.
One DataFusion node with its child left as a placeholder, and the columns this mode says it produces.
One `execute_node` against the seq a recipe named, its handles grouped by the child slot each fills.
One accumulator, not two: the N-into-1 merge is what the parent needs, so the per-lane accumulate-and-sort is not emitted at all.
One aggregate as sql wrote it: the function, and the `ddof` that separates the sample forms from the population ones.
One aggregate's state columns in a partial's output.
One aggregator as both engines name it: the SQL name the executor knows, the call whose arguments it reads, and the output column it fills.
One aggregator call: what it runs, over which expressions, and the columns it produces.
One answer, because the driver pops the batch and the lane consumes it, and the two reading it differently is a join probing its own build side.
One backend per invocation: every category is the same stub type, since what is under test is that the seven associated types resolve and the generic driver monomorphizes — not what any operator computes.
One batch a node emitted.
One batch in one lane is already the whole of every group, so the init node finishes the aggregate itself.
One batch in, one batch out — the contract every `Exec` node keeps.
One batch in, one batch out.
One batch out per batch in, holding no rows — a filter that passed nothing.
One batch out per batch in, unchanged.
One batch per call whatever the reader's own chunking: the plan decided how many batches this lane has, and a node above counts calls.
One batch per lane per producing node, with no cap anywhere: the parent's height is strictly lower, so it drains before the producer runs again.
One batch per lane — the whole chunk arrives at once.
One batch per lane, one per row group and a budgeted size are three different answers to this, which is why the model reads it off the mapping rather than off the budget.
One batch per row group: the finest the mapping can express, since a row group is its minimum granularity, and the only form that needs no budget.
One batch per visit, cycling the lane's map in order from its cursor.
One build -> probe -> finish transition, written once for every backend.
One call at done, over everything the lane accumulated.
One call of a join's recipe: the seq, and the inputs it names in order.
One call per lane event — the shape round-robin driving actually produces.
One call per lane event, and the call carrying the last `Done` is the emitting one.
One call per lane event, since that is what round-robin driving produces, and the call carrying the last `Done` is the emitting one.
One call per mapping entry, and its rows are that entry's row groups — not the reader's own chunking, which would make a lane's batch count the decoder's business.
One call, its named inputs resolved to the handles this join is holding.
One case per field of the mode's own list, so a variant added to `Expr` without a lowering arm is a compile error here rather than a refusal at run time.
One column's declared field, for a caller that needs its name or its type — a key project naming what it kept, a pad literal typed by what it stands in for.
One conscious decision per expression kind; an unrecognized kind is a plan-time error naming it, never a silent pass-through.
One entry per aggregate: the group keys pass through unnamed by this list.
One entry per column the filter's own schema has, in its order.
One expression per aggregate output column, and not per output column: a group key is not finalized and is not here, so this list is shorter than the node's output schema by the number of keys.
One expression, with every column reference as `name@ordinal`.
One field per line, indented under the call that addresses it.
One hand-built node per executor, one hand-written expected result.
One impl per backend, naming a concrete type for the batch and for every executor category.
One in-flight batch at each node, bottom up: a source emits its target, an accumulator emits what it held as one batch per lane, and everything between scales its input by the rows and width it changes.
One input per rule, each built to break the rule it is aimed at.
One instance grows: the total moves by its delta, not by a re-sum.
One instance per (node, lane); the rest are one per node, being the cross-lane points.
One key and one summed column, finalized: the shape the three tests below read, so the list a finalize carries and the row a project has to emit differ by exactly the key.
One kind of failure with a phase, rather than two kinds: every caller that does not care about the phase — the DataFusion conversion, `Display` — keeps one arm, and a pair of variants mapping to the same thing is a pair that drifts.
One lane at tp1, one batch per lane with batching off, whole row groups inside.
One lane holds every group already: v1 skips the shuffle for a one-lane input exactly as it does for a keyless aggregate.
One lane into N by Spark murmur3 on the hash keys — the routing both engines share, so a row lands in the same lane on either.
One lane into N, by Spark murmur3 on the hash keys — the only routing there is, and the same one both engines use, so a row lands in the same lane on either.
One lane is what is left.
One lane of a join has run `set_build`.
One lane's batch delivered to a cross-lane accumulator.
One lane's end delivered to a cross-lane accumulator.
One lane, many batches, no order and no key: cross and nested-loop joins have no key to co-locate on, so nothing about the inputs' layout survives them.
One lane, no shuffle anywhere below either side: there is no hash, so a claim that rows are where a hash put them is false however the keys are named.
One line per node, in plan order.
One mask per grouping set, in key order — true where that key is NULL in that set.
One mask per set: everything, the first key, neither.
One name for one column.
One node of every kind that reaches a backend.
One node over the whole probe side, or one per batch where every emitted row is decided by (build, this batch).
One node, one entry, however many batches it emitted — the worst source in the corpus has 96 in a lane, and a line per batch would make a node unfindable.
One of ours by the name the executor knows it by.
One output short of the lane count.
One path, because the node reads one file: cuDF takes one row-group vector per source, so a path repeated per DataFusion file group answers `Must specify row groups for each source` — or, with no override, reads the file once per entry and concatenates.
One per aggregate sql asked for, naming the state columns it decomposed into — what a merge checks before trusting the positions it is about to merge.
One per script: a query stops at the first failure, so a second one is unreachable by construction.
One per source, in post-order sequence — which is the order translation reaches them, so the second pass consumes them in this order.
One renderer either way: two would drift, and the ten mode goldens and the payload golden would then disagree about a plan neither of them changed.
One row per group, and with no cardinality estimate the worst case is one group per input row (#19 is what would sharpen it).
One rule per category for the whole plan: a test that needs two different emitters is a test about two plans.
One rule runs through it — the ordinal is authoritative and the name comes from the declared schema at that position, so every column reference reads `name@ordinal` and a name disagreeing with its ordinal is visible rather than invisible.
One rule, read by the recipe writer for the wire and by the CPU backend for DataFusion — a second copy of it would be a second answer to which column is whose.
One rule, read by the recipe writer for the wire and by the CPU backend for DataFusion — a second copy of it would be a second answer to which column is whose.
One shape of the same query: how many lanes the scan has, how many batches each holds, and where the scatter puts the rows.
One slot per lane where an executor is per lane, one for the node where it is not — which is what makes a cross-lane node's residency a single figure rather than N.
One sort key: a column ordinal into the declaring node's schema, and its direction.
One source step, written once for every backend.
One spelling per fact — the fb `DataType` a column has, and the flags a decimal carries beside it — because a second one drifts silently: the payload verifies, the bytes differ, and the digest golden is the only thing that ever says so.
One step runs every lane of the node the schedule picked: smallest height, ties leftmost.
One sum over the value column, grouped by the key — the smallest body there is, since what is under test is which executor gets built.
One surviving row group, in file order.
One table's worth of rows, and nothing else about it.
One test per node kind, per expression kind and per planner rule, each from the smallest plan that shows it.
One type with a variant per behaviour, because a backend names one type for the category and what these four do differs completely.
One type with a variant per node, since a backend names one type for the category.
One walk builds that buffer and records, per plan node, the calls it makes and the seqs they address; `llm-wiki/architecture.md` has the table it implements.
One walk of the finished tree: build the buffer, and record per node the calls it makes.
Only an unload's decision is made here, its range being an argument of the driver's own call.
Only an unload's, which is the one operator whose output is not `B::Batch`.
Only one side was scattered on its join key, so the other's rows are not where the first one's are — and an aggregate above would answer per lane for a group that is spread across them.
Only the driver can hold this: an unload instance is one lane's and the count is not.
Only the executors are mock.
Only the node just run and its parent can have changed: a node's readiness is a fact about its inputs, and nothing else moved.
Only the non-default prints: the SQL default is false, which is what nearly every join carries, and a line saying so on each of them would say nothing.
Only the probe slot: a build side with a second batch is the violation below.
Only the two straddling batches are sliced — the rest are forwarded or released where they stand.
Only this part can refuse a plan.
Only what the node carries: a field the writer left unset is absent here rather than printed as a default, since "not set" and "set to zero" are different instructions to the executor.
Only where there are any: a plain group-by writes both empty, and a line saying so on every aggregate in the file would bury the ones that carry them.
Order is load-bearing and not cosmetic.
Order is the semantics: the first true WHEN wins, so a writer that reordered them would answer a different question with the same columns.
Ordering the whole stream is `GpuAccumulateBatchesAndSort`, a different node.
Ordinals into the crossed table, `[build columns…, probe columns…]`.
Ordinals into the crossed table, as DataFusion computed them.
Ordinals into the crossed table, so they are named from both sides in order — the same rule as every other reference.
Ordinals rather than names where the wire carries both, since the ordinal is what the executor reads.
Ours rather than DataFusion's: nothing in a session registers it, and the CPU backend hands the definition to the aggregate it builds.
Output lane k is one branch's lane, forwarded batch for batch, so an order and a one-batch lane survive wherever every branch has them.
Output lane p is lane p of each branch, which is why every branch must carry the same hash: the distribution is what makes lane p of one branch belong beside lane p of the next, and it survives because no row changes lane.
Output lanes are the sum of its branches', and output lane k is served by exactly one of them, so no row changes lane and no lane waits on another.
Output lanes for most, but a cross-lane accumulator becomes ready one input lane at a time and an emitter reads a single one.
Output queues live on the producing node, which is what makes lane remapping free: an emitter reads its child's one lane, a forwarder reads whichever pair its map names, a join reads lane p of each side, and no producer knows who consumes it.
Overshooting instead costs the balance bound: two full row groups against a want of one and a half puts the whole tail in one lane.
Ownership is by move — every executor method takes a batch by value, so reuse after consumption is a compile error rather than an unknown-handle throw from C++.
PARITY BY CONSTRUCTION: we reuse the SAME machinery DataFusion's `ParquetExec` uses for CPU-side row-group pruning — its pushed-down `predicate()` run through a `PruningPredicate` over `RowGroupPruningStatistics` (a faithful port of datafusion-44's internal `row_group_filter.rs`).
Padding is what decides the side, not hashing: a Left join emits its unmatched build rows with the probe columns NULL, so only the build column holds the value that placed every output row; Right is the mirror; Full pads both ways and so has no such column.
Paired by the aggregator's tag rather than by position:
Pairing them by position types both backwards — a sum in a UInt64 and a count in a decimal — which no per-node byte count can show, because both engines read the same declared schema.
Panicking forces a deterministic per-type arm to be added rather than silently producing a wrong/unstable size.
Parameterized by the BATCH rather than by the backend: an event carries a batch, and binding it to a whole backend would make the type unnameable until one exists.
Parquet is written here rather than taken from the corpus, because what is under test is the mapping — which row groups this lane's third call reads — and a fixture whose row groups nobody chose cannot state that.
Partial in both phases, because in this mode an aggregate always emits state: a merge is a partial over state columns, and finalizing is a project above it.
Partial takes values and emits state, Final takes state and emits a value, Single does both at once.
Partitions outermost, batches within them, row groups innermost — and a lane the mapping left empty renders as one, because it is one.
Per aggregator, the Welford state it belongs to, if any.
Per batch it would keep N from each and answer with N times the batch count.
Per batch, from a running count of the rows that have gone past: one entirely outside the interval is dropped, one entirely inside is forwarded untouched, and only the two that straddle its ends are sliced.
Per batch, so partitions are preserved: the collapse to one stream is a node of its own in this mode, never a side effect of a sort.
Per node, how many readiness indices it has.
Per node, its output lane count — what `peak_queued` is bounded by.
Per node, not per category: a limit streams and holds nothing whatever the script says, which is what lets a test put a coalescing parent above a streaming limit — the shape a script with one rule for the category cannot express.
Per node, per child, per that child's lane, the rows this node consumed from it.
Per node, per output lane, rows it emitted that nobody consumed — the queues an early exit left standing.
Per node, per output lane, the batches it emitted in order.
Per node, rows of its input stream seen so far, summed over every lane.
Per node, rows released without an unload call — the saving a limit buys, made visible, since the rows returned look the same either way.
Per node, the most batches its queues held at once.
Per output column of this node, whether it can be NULL.
Per projected column, in projection order.
Per projected column: whether the surviving row groups hold a NULL in it.
Per table, per lane, the batches that lane's loader emits.
Per-column STRUCTURAL byte size: the part that depends only on the column type and the row count, NOT on how rows are split into batches — the validity bitmap plus either the fixed-width data buffer or the var-length OFFSET buffer.
Per-column var-length CONTENT bytes for one batch:
Plain DataFusion planning at tp1 over the committed minimal dataset — this mode translates the physical plan rather than annotating it, so no GPU rule runs.
Planned end to end at one of the three batching forms, which is what decides the mapping the model then prices.
Planning a query in this mode:
Plans built from the real node types, since the driver reads what a node declares — lane counts, categories, intervals.
Positional, and checked by arrow: a column whose type is not the declared one is a state layout this mode and DataFusion disagree about, which is a wrong answer everywhere above rather than an error.
Post-check for a call that consumed its executor — `mark_done_and_fetch`, `finish_and_fetch`, an exhausted source.
Post-check for a call whose executor survived it — including one that changed type, as a join does at `set_build`: the successor reports for the same slot.
Post-order sequence rises left to right across the sources, which is the order translation reaches them, so sorting by it is the mapping between the two passes.
Post-order, so a child's complaint comes before its parent's.
Post-order, so a child's complaint comes before its parent's.
Post-order, so the deepest defect is the one reported: a parent's message names a fix that would not help if its input is already wrong.
Pre-aggregated state merged into pre-aggregated state, emitted at done.
Pre-aggregated state merged into pre-aggregated state, emitted at done.
Pre-call model.
Pre-check.
Pre-order is what the schedule needs and post-order is what the recipes are addressed by; the two are computed here together so nothing has to reconcile them later.
Pre-order makes every subtree contiguous, so the end is the largest end among the children.
Precision and scale stay: an explicit cast's target is unreadable without the state column's declared scale beside it.
Precision and scale, or `(0, 0)`, which the wire reads as "not a decimal".
Preserve the type tag for faithful roundtrip, but the wire payload is identical.
Private to one component, every generated type the crate does not name is dead code — hundreds of them — and `unused_imports` and the clippy set are the same story for code nobody wrote.
Public because the planner is not the only thing that builds a tree: a test that rewrites a planned one into a shape no planner emits needs the same check the planner ran, and the driver does not make it — [`check_canonical_form`] is all it asks for.
Public because the planner is not the only thing that builds a tree: a test that rewrites a planned one into a shape no planner emits needs the same check the planner ran, and the driver does not make it — [`check_canonical_form`] is all it asks for.
Publishing none left the device emitting every build column while the CPU emitted what the node declared, which is one shape answering two ways.
Quoted by its caller on the same rule as a name: a string literal can hold a comma — tpcds q66 concatenates one — and an unquotable token in a comma-separated list is a golden a reader cannot tokenize.
Quoted on the same rule as a name: an interval reads as several tokens, and a plan line is a comma-separated list.
ROLLUP and CUBE, which only an init node expands: the per-position NULL placeholder and the masks that say which positions a set drops.
ROLLUP: the one shape whose state is wider than its group list, because the init emits `__grouping_id` beside the keys and every node above groups on it.
Re-numbered structurally rather than by name: the join knows its side widths and its projection, so an output ordinal is derivable even where the key name appears twice in the output or not at all.
Reaching these through a whole plan works but says nothing about which of the two is wrong when one breaks.
Read back rather than described from the plan node it came from: a description written beside the writer agrees with the writer by construction, and would keep agreeing on the day the writer became wrong.
Read back through the generated accessors rather than compared as bytes: what matters is that the C++ reading this buffer finds the expression we meant, and a byte compare would pass on two spellings of the wrong thing as readily as on the right one.
Read before an executor exists, since runnability asks for it, so it is derived from the node rather than from what a backend returned.
Read by the test that holds the two readers of that rule to one answer; the rule itself is `JoinCapability::answers_in_one_call`.
Read by the tests, which are what pins the doubling: without it a disjoint-key aggregate compacts once per arrival.
Read here and handed to the recipe function rather than reached for by it: what the function may not do is walk the tree, and a node's own declared input arriving as an argument is not walking.
Read here rather than guessed so a test can watch the headroom.
Read only by [`BatchSizing::Budgeted`]; the other two forms reproduce a plan from the data alone.
Read row-group metadata (sync; the parquet is local at serialize time).
Readiness for every node, and the limits settled: a zero-row interval is satisfied before anything runs, and the plan still has to complete rather than stall.
Reading the group list as the key width shifts every state position by one: the triple is then found under the aggregator after the one that owns it, its first aggregator is left unclaimed, and the wire is told about a `count` that nothing on the far side has a state column for.
Reading the recipe plan back: the node at a seq, by the same post-order the C++ indexes with.
Reading the size once is what keeps the released figure equal to the held one, and what keeps an arrow batch from walking every array twice per hop.
Reads its lane's row groups out of the partitioner's mapping.
Realized partition count for [`TargetPartitions::Multi`].
Recompute this node's ready lanes.
Red if the limit ever accumulates: a skip of 3 over a batch of 2 would hold that batch.
Red with `safe: true`: the batch comes back with a NULL and no error at all.
Red without the override: two rows for `a` instead of one.
References inside `aggs` index the node's input; references inside `finalize` index the node's own intermediate table, `[group keys…, state columns…]`.
Refuses an anti or mark join whose NULLs can meet under SQL semantics.
Refusing on that would turn "we do not know" into "you cannot run this".
Released here rather than shipped and trimmed, which is the whole saving on an offset: the prefix a skip drops is unbounded.
Rendering a plan as text: one line per node, indentation as the tree.
Rendering one expression as text.
Resident-memory budget tier handed to `GpuMemoryBudgetRule`.
Returns the bytes and how many fb nodes went into them — stubs and structural unions included, since the C++ indexes those.
Returns the modelled scratch so the matching `end_*` can compare it.
Root-adjacent is the other lowering: the interval belongs to the unload, which is what lets the driver release a batch it wants none of rather than move it and throw the rows away.
Round-robin carries no key, so it says nothing this mode acts on and leaves no node.
Round-robin carries no key, so it says nothing this mode acts on; an unknown partitioning is a claim we cannot read, and guessing at it would be a lane assignment nobody chose.
Round-trip one expression: written as the buffer's root, then read back.
Rounds down onto a coarse grid.
Routes whole batches into a new lane numbering; never touches rows, never buffers.
Routing cannot retype anything, so a branch that does not already match the declared output is a missing project rather than work for the executor (#41).
Routing cannot retype anything, so a branch whose columns differ from the declared output is a missing cast rather than something the executor should fix up (#41).
Routing does not touch a batch, so an order that holds within each branch's batches holds within the output's.
Routing is the driver's, so a forwarder is answered before a recipe is looked for: its absence is what a forwarder means rather than a gap to report.
Row groups with the file indices they would have after pruning dropped groups 0 and 3 — the mapping carries file indices, never positions in the survivor list.
Row-group metadata for the partitioner, read at plan time.
Rows 10..40: batches 1, 2 and 3 are wanted entire, and no range rides the call.
Rows 25..35 of ten-row batches: batch 2 straddles the start, batch 3 the end.
Rows a node emits.
Rows and bytes, which is all any assertion here reads off a batch.
Rows move, are dropped or are re-ordered; a column that could not be NULL below still cannot be above.
Rows per arrival.
Rows spread evenly over the lanes.
Rows the finish pass emits.
Run `root` to completion on `B`.
Run `root` to completion on `B`.
Run `root` to completion on `B`.
Run one node — every lane of it.
Run to completion under no budget, which is what most flow cases want.
Run-configuration tiers shared by the executors, the test harness, and the golden-file device labels.
Runnability asks the same question the driver later acts on, and a predicate that borrowed the input queues could not be asked while the executor is held mutably — so [`LaneDriver::select`] reads an availability summary and the caller hands over the batch it named.
Running a plan: the contracts a backend implements, the batch that crosses them, and the driver that walks the tree calling them.
Runs before the generic structural rules, because a node can name the fix where a generic rule can only say what is wrong.
Rust sorts under twenty elements in place, which is stable whether or not the sort claims to be — so a shorter case pins nothing about the sort it names.
SCOPE: single-source scans with a static pushdown predicate (TPC-H q6/q1/q14/q15-style).
STATS-ONLY pruning: this uses `PruningPredicate` over column-chunk min/max/null statistics, NOT the separate bloom-filter prune step cuDF/DataFusion can also do.
Same absence as [`Input::BatchCopy`]: the first call gets the handle and a second is refused naming #152.
Same predicate + same row-group stats => identical surviving set.
Same scale, because a scale difference moves the point and is a different number rather than a wider one; and wider only, because narrower is not what a merge does and casting it up would be inventing precision the state never had.
Scattering cuts every batch into N, so neither an order within a batch nor a one-batch lane survives.
Search form leaves `comparand` `None`; the value form sets it.
Selectivity and join cardinality are the trivial estimates the planner has today, so a filter passes everything and a join is 1:1 against its larger side; a limit is the one node that knows better.
Seqs are the post-order positions of the tree that was built, stubs included — which is why they are not dense, and why they cannot be counted from the recipes alone.
Seqs ascend with the order the nodes are built, which is the order the C++ indexes them — `writer.rs` has the three rules that keep those the same sequence.
Seqs ascend with the order the nodes are built, which is the order the C++ indexes them — `writer.rs` has the three rules that keep those the same sequence.
Shared by the group names and the NULL-placeholder names, which the wire keeps parallel.
Single source of truth for node `output_bytes` on BOTH backends — the GPU backend reconstructs its stats through `logical_size_from_schema` — so CPU and GPU costs are identical by construction whenever per-node row counts match.
Single-partition and single-use: the stream is taken on the first `execute`, and a second call errors rather than returning an empty one, which would read as a node that produced nothing.
Single-source only — cuDF set_row_groups is per-source; keep scope tight.
Six batches of ten rows on one lane, over a chain the interval sits on top of.
Six rows in three row groups of two, written once per process.
Slow and obviously correct, which is the whole point.
So Left's one call may consume the build outright.
So an executor holds a borrowed session pointer, its recipe's calls, and the schema its output is priced by.
So arrivals are held until they cross the threshold, folded once, and the threshold set to twice what that fold left behind.
So the children are replaced by a source that replays the batches the call was handed, and the node is executed against those.
So the merge runs over no input rather than being skipped, and the operator answers: one row where there are no group keys, none where there are.
So the plan reaches all three — a scatter and a sorted merge under the probe side, with a routing merge between them.
So this backend emits nothing and the empty batch is the driver's to supply, which is the one place that knows the schema without asking the device.
So this casts rather than relabelling, which is what the shared `declared_as` does for a node whose operator already produced the declared types.
Sorted as the batches arrive, merged once at done: the merge arm takes whatever k handles the call hands it, so the lane's batch count is a runtime number.
Sorted batches concatenated are not a sorted batch; only a merge makes one.
Sorted within each batch and not across them — what a `GpuSort` alone leaves, and the shape a prefix of the stream is not the top of.
Sorting each batch leaves them individually ordered and collectively not; the accumulator is what makes the stream sorted.
Sorts each input batch independently, so the batches are individually ordered and collectively not — an accumulator above it is what makes a stream sorted.
Sources also print the batch size they were given, which is the one number the partitioner reads back.
Sources are reached in one order, left to right, so the estimator's figures are consumed in that order — the same order it indexed them in.
Sources are scripted per table name and per lane; every other category takes one rule for the whole plan, which is what keeps a test's setup readable.
Spark `pmod` (positive modulo): a signed murmur3 hash into `[0, n)`.
Spark's `HashPartitioning` seed; comet and the GPU kernel both initialize to it.
Split as `[(2,20), (2,21)]` then `[(4,40)]`:
State *types* come from DataFusion's `state_fields()`, so the split cannot drift from the one DataFusion planned; the state *names* are ours, since the golden and every later reference read them.
State and model as two numbers the test sets, which is all the accountant reads.
State from raw values, and the finalize where the node carries one — two operators, as the recipe is two calls.
State from raw values, per batch — and the finalize where the translation gave this node one, which it does only for a single-batch lane, where one batch is already the whole of every group.
State held between calls.
Stated rather than reached by making the input large: a threshold in bytes and a fixture in rows are two facts that drift apart, and the test would go quiet rather than red.
Stop where taking the next group would land further from this lane's share than stopping does.
Straight through to the fetch's row range.
Streaming emission is #138.
Streaming: one scatter call per input batch, N outputs, some of them empty.
Subset, not equality — a grouping-set rollup hashes on the keys while grouping on keys plus __grouping_id.
Subtracting in the count's own unsigned type never reaches that arm — cuDF wraps past it and answers a value, arrow refuses the mixed subtraction outright.
Subtree presence, not a path: which nodes may carry a claim past themselves is a rule each node states about its own output, and this is the cross-check that the chain starts somewhere real.
Subtrees are contiguous because the numbering is, which is the property every hold range rests on.
Surviving row-group indices for `parquet`'s single source under its pushdown predicate.
Survivors → partitions → batches → row-group indices.
Taken by the node being built, so it does not stay in the pool.
Taken from the walk the driver already makes rather than derived a third time.
Ten distinct keys over forty rows, so every key is tied four ways.
That index is a position in the TREE and a [`Seq`] is an address in the recipe plan: they part company at the first node with two calls, and a driver holding both at once has to keep them apart.
That is a defect no inequality can see, which is why it sits beside `in_flight_bytes` returning to zero and `holds == releases` rather than in place of them.
That is also what keeps the tier boundary structural: a rust-only target instantiates the drivers only at the CPU backend, so the GPU types are never named.
That is not a saving so much as what lets this type exist at all: summing over live executors means holding a reference to each, and the driver owns them mutably.
That is only true if the same key goes to the same lane every time it is seen.
That is the assertion the defect was.
That is what a set operation wants and what SQL's `NOT IN` does not, and the difference only shows where NULLs can meet — which is why the refusal reads the data rather than the declared types: every column in both benchmarks is declared nullable, keys included.
That is what lets the two backends and the goldens agree on what a batch is.
That is what makes a scheduling corner reachable from a test that builds no plan.
That single rule is what makes this a push model — a batch's parent is strictly lower, so it is carried up before anything below produces again — and what bounds every queue at one batch per lane without a cap.
The (child index, child lane) pairs feeding output lane p, in service order.
The *realized* partition count is plan-derived and only ever ≤ this hint (a scan over fewer files, or a coalescing parent, lands below it).
The 1:1-per-batch nodes: filter, project, and the per-batch sort.
The ABI clamps an offset past the end and a length past it, because a C ABI has to be total — and `range_of` can produce neither, so reaching that tolerance means `rows_seen` has drifted.
The API is not the same in every feature shape.
The C++ side never sees the plan tree.
The CPU backend relays to DataFusion, so it needs the expressions back in DataFusion's own vocabulary.
The CPU backend's batch, and what leaves the device at the unload.
The CPU backend's executors: one DataFusion operator per node, run one batch at a time.
The CoalesceBatchesExec DataFusion puts above the filter leaves no node: its target says nothing about the batches a lane will hold in this mode.
The GPU backend looks a recipe up by the post-order it is given here, and a wrong number reaches it as another node's recipe rather than as an error — so the claim is checked against a walk written in this file, not against the one under test.
The GPU backend's executors: a node's recipe is the instruction set, and running it is all these do.
The IR is neither planner nor executor — it is what passes between them, and consumers span both halves.
The NULL substituted for each key a set excludes, in key order.
The Welford state as DataFusion declares it, in its order: a count and two doubles.
The Welford triple is three of this mode's aggregators and one of DataFusion's, so what proves the fold is that one aggregate filled three declared columns — a triple sent as three separate aggregators would fill three columns of its own and disagree with the declared state's width.
The `--- memory ---` section: the estimator's per-node figures under the same tree the plan renders, so a line reads against the node above it.
The `--- memory ---` section: what the estimator predicted, per node.
The `--- recipes ---` section: what each node asks of the device, under the same tree the plan renders, so a line reads against the node above it.
The `--- recipes ---` section: what each node asks of the device, under the same tree the plan renders, so a line reads against the node above it.
The `Option`s are the capability matrix in the only form an executor needs it: a call it does not make is a call it does not have.
The `allow` is not cosmetic here, which it was while this module was `pub` in `lib.rs`: everything was externally reachable then, so `dead_code` could not fire.
The `count - ddof` both arms rest on, read off the finalize rather than rebuilt, plus the zero it is compared against.
The `hash_keys ⊆ group columns` rule a final aggregate's input must satisfy: rows of one group are co-located only when the shuffle keyed on a subset of the columns being grouped.
The `is_null` flag is what distinguishes it from a zero value on the wire.
The accountant reads `scratch_bytes` BEFORE the call, so a transient charged for a read that never happens refuses a query that fits.
The accumulator below it emits a single batch in a single lane, so the init node is already looking at whole groups and finishes them itself.
The accumulator holds 3200 and is about to build another 3200 out of it.
The accumulator includes what it holds in its pre-call model, which is what the spec permits by letting `scratch_bytes` consult `&self`.
The accumulator is DataFusion's own with one method rewired: what arrives is state, so `update_batch` merges it.
The accumulators on a device: what each one holds between calls, and which of its recipe's calls it makes when.
The accumulators: the nodes that hold a lane's batches back, and the mid-plan limit, which is one by category and holds nothing at all.
The accumulators: what each one holds, and what it emits once its input is complete.
The additive entry point, and the reason it exists: the node's own row-group list is overridden per call, so one seq serves every batch the mapping cut the scan into.
The additive symbol is what makes the node a source at all — the frozen `execute_node` reads every row group the scan node carries, in one call — so the row groups a batch reads are an argument here rather than a field of the node the seq addresses.
The aggregate above the scan is where a batch is widest — its output row carries a key and a count where the input row carried a key — and that is what sizes the source, though it is neither end of the walk.
The aggregate as DataFusion runs it: the group list under the names the state gives them, and one SQL aggregate per [`state_funcs`] entry — which is what makes the state this produces the state the node declared, three columns at a time where a Welford triple is one aggregate.
The aggregate here is the one node kind the list holds twice — `GpuAggregate` and `GpuAggregateBatches` are different kinds — so the cover is over kinds, not over the list's length.
The aggregate is the node whose schema carries the annotations a merge reads, so a width slip there mis-numbers the state columns rather than only the output.
The aggregate nodes.
The aggregate payload, which is the one kind whose vocabulary differs on the two sides.
The aggregate sequence: what a partial declares, decomposed into the parts each position needs.
The aggregate vocabulary and the decomposition registry.
The aggregate's state is held whatever the batch size, so it is spent before anything is divided, and what is left is what the source may spend.
The aggregators a body declares over a state schema, in the order their state columns appear.
The aggregators a body declares over a state schema, in the order their state columns appear.
The aggregators and the optional `final` list every aggregate node carries.
The aggregators as the wire declares them, in the order their state columns appear — which aggregate is which, and what it is called, is [`state_funcs`](crate::plan::state_funcs); this writes what that names.
The annotations are what a merging or finalizing node checks before trusting a position — the class #135 describes, where an ordinal read in the wrong order produces identical per-node numbers everywhere and surfaces only in the final result.
The arm is here so both paths agree if that ever changes — one query must not have two plan shapes depending on where the fetch was parked.
The arrow schema is shared rather than rebuilt where a node passes its input's columns through, which is what makes "the types are DataFusion's own" true by construction rather than by a second derivation agreeing.
The backend has no executor for this node, or the set it returned drives a different category than the node is.
The batch is gone by move, and its release is skipped because C++ has erased the registry entry: releasing again would be a use of a dead handle.
The batch the call was scheduled for.
The batch went into the call either way, so its bytes leave here whether or not the call came back.
The bound holds for what a parquet writer emits: one row-group size per file and a short last group.
The branch is live.
The buffer a join's recipe addresses, so a case can read the payload rather than the call list.
The build rows nothing matched, padded out to the joined schema.
The build side and one more column, the mark.
The build side ended with no batch.
The build side finished without a batch — this lane's scatter gave it no build rows, which a small table over many lanes produces routinely.
The build side for as long as this join holds it, and the probe keys a finishing type keeps until its finish pass runs.
The build side for as long as this join lives, and the probe keys a finishing type keeps until its finish pass runs.
The build side is always the left child, which is the orientation the schedule turns into "the build subtree drains first".
The build side is complete before the first probe call, so a probe row unmatched in this batch is unmatched everywhere; only build-side rows need the finish.
The build side is one resident table, which is what CollectLeft means here as well as on the device.
The build side only where a probe call reads it: the build-side semi family's probe call is the key project, and charging it the build side would refuse a query that fits, since the accountant reads this before the call rather than after it.
The build side only where a probe call reads it: the build-side semi family's probe call is the key project, and charging it the build side would refuse a query that fits, since the accountant reads this before the call rather than after it.
The build side set, both probe batches through, then the finish — the call sequence a driver makes, in the order the typestate allows.
The build side, from `set_build` until the call that consumes it — `None` after, because the surface has no copy and the recipe says which call takes it.
The build side, handed over: the call consumes it and nothing needs it again.
The build side, resident from `set_build` until the call that consumes it.
The build side, where the recipe asked for a copy of it.
The build side, which is one batch per lane.
The build side, which is one batch per lane: the planner puts a `GpuCoalesceAllBatches` under it, so this is every row it will ever hold.
The build-side semi family keeps its finish pass and gives up streaming instead.
The build-side semi family makes no per-call join at all: its probe call is the key project alone, so the build side is untouched until the finish consumes it.
The build-side semi family's finish emits the row itself — build columns, and a mark for a mark join — so it needs a project only where the node narrows it.
The build-side semi family's output IS the row, so it needs a project only to cut a projection down: a LeftSemi over three build columns declaring two of them, which is where q20 was answering with a column the plan did not ask for.
The build-side semi family's probe call is the key project alone — it does not touch the build side — and this charged it anyway until the review found it.
The build-side semi family: no per-call join at all, so the whole answer is the finish pass over the keys the probe batches contributed.
The bytes balancing is not the counts balancing: a release of nothing subtracts nothing and still counts, which is how a cross-lane accumulator's lane-done events made these disagree with the invariant the report states.
The call CONSUMES them, so a caller hands over batches it will not release itself.
The call delivering the last lane's `Done` is the emitting call.
The call ran and kept far more than it said it would need.
The canonical-form rules a driver needs to have been applied before it runs, so a mock plan meets the same refusal a planned one would.
The canonical-form rules a driver needs to have been applied before it runs, so a mock plan meets the same refusal a planned one would.
The case the field exists for: cuDF's divide scale is s_left - s_right and DataFusion's is not, so the declared type is how the C++ lands on the right one.
The cast is a node rather than something the executor does behind the plan's back (#41), and it is the same rule as everywhere else: every cast is explicit.
The category check above passed, so these cannot be reached from a lane-scoped node; a backend that returns one is caught there.
The category claim is the one the driver cannot make for itself: it checks the category it was handed against the node's, so a backend that builds the wrong executor is caught at run time and named — but only for a node some plan actually reaches.
The child order `NodeSession::node_children` walks, which is what makes a seq mean the same node on both sides.
The child's lane count, which is how many `Done` events a partition accumulator owes.
The claim the matrix makes about this family, as a check rather than a sentence: its per-batch call touches neither the build side nor a copy of it, which is why it streams at no copy cost at all.
The class an exhausted budget has always raised, so a caller distinguishing an over-budget query from a broken one keeps doing it by the same match.
The collapse arm does answer a call with no handles — but with a table of no columns, which is not the batch of the node's schema a SingleBatch output owes downstream.
The collapse arm: it concatenates whatever k handles the call passes, which is why the node carries nothing but its input.
The columns a state leads with before the first aggregate's: the group list, plus the `__grouping_id` an init expanding grouping sets emits beside the keys and every node above it groups on.
The columns a state leads with before the first aggregate's: the group list, plus the `__grouping_id` an init expanding grouping sets emits beside the keys and every node above it groups on.
The columns the node's projection keeps, out of what the finish emitted.
The combine is not a per-column reduction, so it is one call over all three.
The committed minimal dataset, whose `p_retailprice` and `c_acctbal` are `Decimal128(15,2)` — the two columns every decimal assertion below starts from.
The conservation law, lane for lane: every row a node emitted was taken by its parent or left standing by an early exit, and a row in neither vanished.
The contract is one batch out per batch in.
The corners are enumerated rather than sampled, and then a differential test asserts the incremental schedule agrees with a naive rescan on randomized shapes: if the two disagree the incremental one is wrong by definition, which is the only cheap way to keep it honest as the holds grow.
The corpus goldens are a regression net over whole plans and a different question from whether a rule is right.
The count above says a set was built; this says which node the driver thought it was building for, which is the only place that number is observable at all.
The count is cast before anything is subtracted from it, and the two literals are floats: an Int64 ddof puts the subtraction in the count's own unsigned type, where a count below ddof wraps instead of going negative.
The count is the contract: a driver reads output `p` as lane `p`'s, so a skipped empty would shift every lane above it.
The count is the contract: a driver reads output `p` as lane `p`'s, so an empty lane still owes its batch — skipping it would shift every lane above it.
The cpu backend guards on the same capability at `cpu_backend/join.rs`, and one question answered two ways is how the two backends drift.
The cross-lane accumulator: four lanes in, one out, and `consumed` indexed by the lane each batch came from.
The declaration is the fixed point, and it is the precision the device is sent, so the cast back is what keeps the two engines' states one type rather than one value in two.
The declared decimal output travels with the op: cuDF derives its own fixed-point result scale, division most visibly, and the C++ reproduces DataFusion's from these two rather than re-deriving the coercion.
The declared names are what every name@ordinal check above the union resolves against, so a branch that names the same column something else makes one of the two readings wrong wherever they meet.
The declared schema is a plan fact and belongs to the plan golden alone — recorded once per plan rather than repeated in every query's execution section.
The declared schema is the plan golden's alone and what the run produced is the execution golden's, so each caller appends its own tail.
The denominator is an exact integer-valued decimal of the same precision, so cuDF's own divide scale (s_left - s_right) lands on the scale declared above rather than on one it derived.
The denominator is cast to an exact integer-valued decimal, so cuDF's own divide scale (s_left - s_right) lands on the scale DataFusion declared.
The depth the C++ verifier allows, since a recipe plan is a chain and is as deep as it is long (#169).
The design rather than a state of affairs: a model that prices nothing gets no protection, and always will not.
The device batches carry the script's sizes; the unload's are the host's, priced from the plan's schema like everything else — ten Int64 rows are 80 bytes and a two-byte bitmap.
The device sorts each batch and merges the runs, which is two calls; here it is one sort over the concatenation, and the same answer — a stable sort of the runs in arrival order puts tied rows exactly where merging them would.
The distribution a join declares about its own output, which is the claim nothing downstream re-checks: a co-partitioned join above it is refused or allowed on this.
The distribution the output carries.
The doubling, shown by the two regimes it exists to separate.
The driver adds the node and the lane and fails the query — a retry with a smaller batch is #142's adaptive future and not this design.
The driver asks rather than deciding, because what a lane owes is a property of the join type and the executor is where that lives.
The driver checks it against the node's own category, so a backend wiring a node to the wrong trait fails where it was built rather than at the first call that finds the wrong method.
The driver owns every one; naming them is what makes a recipe checkable against consume-on-use (#152), since a copy appears here as a copy.
The driver reads both figures already — the rows for a limit interval, the bytes for the accountant — and kept only totals until the corpus goldens needed the sizes themselves.
The drivers are generic over [`Backend`], so each backend monomorphizes — no vtable, and `Drop` on a GPU handle is a direct call.
The early-exit path deliberately ends with lanes not done and queues non-empty, so drainage is the wrong invariant; what has to hold is that every batch held was given back.
The enforcer through the drivers: what trips, what is merely recorded, and that a finished run gave everything back.
The engine: a lane holds a stream of batches rather than one resident table.
The exception is a global aggregate, which owes its identity row whatever arrived — see [`AggregateBatches::mark_done_and_fetch`].
The exec executors: a filter, a project, a per-batch sort, an aggregate with and without its finalize, and the unload's row range.
The execution golden's text, over the mock backend so every number in it is chosen.
The execution golden's text: the plan tree, and what the run produced at each node.
The execution golden: the plan with what each node actually ran under it.
The executor contracts, one per node category.
The executor overrides the thresholds, and this asserts what the executor DOES rather than what the context said — the difference being that a later site building an executor from an unguarded context still goes red here.
The executor pointer is BORROWED, as everywhere else on the GPU path: the session outlives every batch drawn from it.
The executor says whether the lane owes nothing — six of the nine join types do — and the lane drains from there.
The executor, in whichever state it is.
The export does not consume the handle, so the batch is released here by going out of scope — which is the whole of what the row range buys: the rows wanted cross PCIe rather than the batch they sit in.
The export holds nothing and its transient is the rows crossing out — the IPC buffer the range names, which is why a limit's range is worth having.
The expression IR.
The expression as the buffer holds it.
The expression that turns merged state into the aggregate's output column.
The expression that turns merged state into the aggregate's output column.
The failure a real backend reports: a message, and nothing to resume from.
The fb payload each addressed node carries, one function per kind.
The fetch as a slice of an ordered batch — see [`SortedRuns`] for why it is not DataFusion's top-N.
The fetch is a slice of the ordered batch rather than `SortExec::with_fetch`, for the reason the accumulating sort gives: a top-N keeps a bounded heap, so which of two rows tied on the keys it kept depends on the heap rather than on the plan.
The fetch is a top-N over the stream.
The fetch is applied by slicing rather than by `SortExec::with_fetch`, and that is the point: the top-N path keeps a bounded heap and does not preserve arrival order among ties, so which of two tied rows a limit kept would depend on the heap.
The fifth, appended to the schema for a stddev's finalize.
The file's own numbers, read independently of the code under test.
The filter is written against a schema of its own — neither side's, and not the joined one — so its ordinals mean nothing without this map.
The filter is written against a table of its own, so its ordinals travel with the map that says which side each of them came from.
The filter's ordinals index a table of its own, which appears on no line.
The filtered forms are one legacy call over a probe side the planner made single-batch.
The finalize as the project it becomes — the columns are [`finalize_columns`](crate::plan::finalize_columns); this writes what that names.
The finalize as the project it becomes: the group keys straight through, then one expression per aggregate output column, named as the node declares its output.
The finalize as the project it becomes: the group keys straight through, then one expression per aggregate output column, named as the node declares its output.
The finalize is this mode's own expression, evaluated by DataFusion rather than by a `Single`-mode accumulator — the same expression the device is sent.
The finalize project, where this node finishes the aggregate.
The finalized output holds the keys where the intermediate did and the finalized columns where the state was, so the keys are still annotated and the state is gone.
The finalizing merge emits the aggregate, so the keys are still keys and there is no state left to declare.
The finest the mapping can express, and it needs no target to express it.
The finish join, against the accumulated probe keys.
The finish join, against the accumulated probe keys: their ordinals are `0..k`, since the key project emitted the key columns and nothing else.
The finish pass emits the columns the node's projection keeps, in its order — not every build column with a pad after it.
The finish pass holds the key columns of every probe row it has seen (#136), and the small table on the left is what keeps this a Left join — DataFusion swaps the sides, and remaps the type, when the right one is smaller.
The finish pass is the half that has to answer in the node's declared shape: the anti join emits every build column whatever the projection says, so what the pad project keeps is the whole question.
The first pass assumes one number for every source and is used only to derive the real ones; the second is the plan.
The first probe batch can have the original; a second has nothing to be given, because the call that read it erased it.
The first rule — a node takes the most recently created unconsumed offsets, so its children are the contiguous block immediately below it, which is what post-order means.
The first walks from every source up to the nearest accumulator, because that is exactly where resident stops scaling with batch size — a join's build side holds a whole relation and an aggregate's state one row per group, so those come off the budget as constants before anything is divided.
The five cells the matrix refuses — the three outer forms and the two right-handed semi ones — each for a defect or a missing cuDF variant rather than for anything this mode decided, so each names the ticket a reader would follow.
The footer, parsed once: a lane reads the same file once per batch, and re-parsing it per call is the whole of what a scan does besides decoding.
The formula, the cache, and the two checks.
The four lane-scoped categories plus unload get one executor instance per (node, lane), and this is that instance's state machine: it decides which call the lane's current input state calls for, makes exactly one, and reports whether the lane will ever produce again.
The four types that emit both sides.
The frozen entry points.
The group list alone is one short of the state exactly there, and a state position read one column early names the aggregator before the right one.
The group list alone is one short of the state exactly there, and a state position read one column early names the aggregator before the right one.
The grouping-set init emits `__grouping_id` beside its keys, so the state is one column wider than the group list.
The guard is reached at stream construction (see `assert_type_accountable`), NOT in a destructor, so it unwinds as a normal test failure instead of aborting the process.
The handle is the whole value — no box, no vtable — and `Drop` releases it, which is what keeps a batch the driver abandons from leaking VRAM.
The handles a call is about to consume, and the batches they came from dropped without releasing:
The handles are not touched again, but the totals still reconcile: the release path is the same one the early exit uses, so held equals released on every way out.
The handles differ — a single-batch probe hands the build over, a streamed one copies it (#152) — and the plan does not.
The handles go into the merge in lane order, which is what makes a tie partition-major rather than arrival-ordered.
The hash a branch carried says nothing about the union's numbering, so it goes.
The hash does not: lane k means something different in each branch's numbering.
The hash is [`crate::spark_partitioning`]'s, which is the device kernel's — so a key lands in the same lane on either backend, and the per-lane counts a golden records are the same numbers.
The hold lifts when every lane has, since a lane still building still cannot take a probe batch.
The init and the per-lane merge both emit state: one group key, then the two columns `avg` decomposed into.
The init node runs the three Welford aggregators over raw rows.
The init's aggregates: one per [`state_funcs`] entry, resolved by the name both engines know it by, which is what makes a Welford triple one aggregate of three state columns.
The init's mirror of the merge below.
The input arrives already accounted — it is alive on the device while the call runs, so the pre-check counts it — and outputs leave here held, so the caller only queues them.
The input handle is consumed by the first call and every intermediate by the call after it, so what is released here is nothing: a failed call ends the query, and the session it belonged to is torn down with it.
The input went into the call that failed, so its bytes leave the accounted set here rather than after the call that would have released them — the totals still reconcile, and the release error is dropped because a failure is already on its way out.
The input's hash as the group list re-numbers it.
The inputs are the optimizer's estimates, and an estimate that drifts slightly should not regenerate every golden.
The interval rides the boundary crossing, and prints there.
The interval rides the boundary crossing, so the skip prefix never moves.
The join a probe batch runs: the node's keys and residual, and the type the call emits — the node's own where nothing finishes, the per-call one where something does.
The join a probe batch runs: the node's own keys and residual, and the type the call emits — the node's for a probe-local join, the per-call one for an outer.
The join capability matrix, run: one row per mode, each against rows written down here.
The join the finish pass runs against the accumulated keys.
The join the finish pass runs against the accumulated keys.
The join this probe batch runs, if any — absent for the build-side semi family, whose probe call is the key project alone.
The joined table, before any projection: build columns then probe columns, with the unpreserved side padded.
The joins on a device: the calls the recipe names, and the one it names that the frozen surface cannot make.
The joins on the CPU: the decomposition the recipe names, one DataFusion operator per call it names.
The joins, which is where the mapping has structure: a streamed probe cannot know which build rows matched, so the types that owe their build side a row keep the probe keys per batch and answer the question once, at done (#136).
The joins.
The joins: what each of the three requires of its inputs, and the distribution a join declares about its own output — the claim nothing downstream re-checks.
The key columns a finish pass accumulates, per probe row.
The key columns, in the layout comet's hasher accepts.
The key project builds the accumulated keys table and names its columns; the finish join is its only reader, so the name it uses to read them has to be the name they were given.
The key project's output is the lane's growing key table, not an answer.
The key table this lane accumulates is the probe's key columns under the probe's names, which is what the key project the recipe names emits.
The key, or `total` where the set dropped it, paired with what that row summed.
The keys are taken from the state by position, so a state whose first columns are not the keys fills the row to the right width out of the wrong columns.
The keys are the half the finalize list does not carry, and a project replaces the row: finalized columns alone answer with values and nothing to read them by.
The keys each set groups on — the complement of the mask, which is the half a reader checks against the rollup the query asked for.
The kinds whose recipe is more than one call, `GpuHashJoin` first: per join type, the seq set it emits and when each call is made, against the capability matrix.
The lane count is the one thing a recipe repeats from the plan line, because it is a field of the node the call addresses rather than a fact about this node's output.
The lane count rides the call's own kind, which is where the recipe repeats it from the node it addresses — so the executor and the fb node cannot disagree.
The lane count survives, and so does a hash the input earned: a join does not move a row between lanes.
The lane state machine on its own: one (node, lane), one call at a time, with no tree and no schedule around it.
The largest a batch from this source gets on its way to the accumulator that ends the walk, counting the lanes live at that point.
The largest a stage's ANSWER got — not the largest allocation the call made, so a sort's working buffers are outside it.
The largest batch the mapping produces.
The last lane's share is everything left, and every group takes it closer to that.
The law they rest on is `consumed + abandoned == emitted`, per the child's own lane: a row a node emitted was either taken by its parent or left standing by an early exit.
The layer's contract is that the rows it hands back are the ones DataFusion planned, and nothing else states it: every node below is checked against its own children, so a whole tree can be internally consistent and answer a different query.
The layer's contract is that the rows it hands back are the ones DataFusion planned, and nothing else states it: every node below is checked against its own children, so a whole tree can be internally consistent and answer a different query.
The layer's contract with its caller: every node below is checked against its own children, so a tree can be internally consistent and answer in a type the query did not ask for.
The layout a node inherits from its input.
The leaf of the null analysis, and a statistic rather than a declaration.
The leaf: what the surviving row groups actually hold.
The left form pads the build rows no pair kept — and it can, because the matrix gives it a single-batch probe: with one call there is no later batch for a padded row to be wrong about, which is why it needs no finish pass of its own.
The legacy node kinds this mode addresses, carrying the fields a reader cannot get from the plan line above: a per-call join type is not the node's own, and a repartition's lane count is the one number a recipe repeats.
The loader: a lane reads the row groups the mapping gave it, one batch per call.
The map arm; a top-N's `fetch` rides the node, so it trims each batch and whatever accumulates above it trims the merged result.
The mapping addresses one file's row groups, so a second file would be sized from the first's — a wrong answer where an error belongs.
The mapping for a known input, pinned.
The mapping is stored verbatim — partitions outermost, batches within, row groups innermost — because the loader executes it, the golden prints it and validation counts lanes off it.
The mapping is what the driver reads the lane count off; how many batches each lane emits is the mock's script.
The mapping verbatim: partitions outermost, batches within them, row groups innermost.
The mapping, one arm per row of the table.
The mark column is a boolean the join computes and never NULL.
The mask maps what the projection names: column 1 alone comes back as one column, and it is the value column rather than the key the reader would hand over first.
The matrix's second dimension.
The maximum rather than either end — a batch is rarely widest where it starts, and above a merge the same batch costs one lane's worth rather than N.
The merge a `GpuMergeSortedPartitions` runs, which is the same node one level up: k sorted lanes into one stream, the `fetch` applied to the result.
The merge across lanes, past the cutoff: every lane's rows arrive, the keys come out ordered, and the answer does not move between runs.
The merge body an avg's state takes: a count merges by sum, which is the one place naming the merge separately from the init is the difference between a right and a wrong answer.
The merge's aggregates, which the wire and this side spell differently.
The merge's round-robin and the interleave's child rotation are this one rule.
The mid-plan limit: it streams, holding nothing.
The mid-plan limit: it streams, holding nothing.
The mid-plan node is an accumulator by category and streams: the driver counts the rows going past it, and satisfaction stops the scan the same way it does at a sink.
The mock, against its own script.
The mode's [`Expr`] → DataFusion `PhysicalExpr`, the inverse of [`expr_translate`].
The model above is what closes it.
The model is a byte of scratch per input byte, so the first real call asks for twice what one batch costs.
The name a schema declares at that position.
The name beside the ordinal is the check, not decoration: a rebase that moved a column and left the name behind reads a different column, and every value it returns is the right type.
The name is redundant on purpose: an ordinal read in the wrong order is otherwise invisible until the final result (#135), and the layer rebases ordinals at every node it inserts, so a stale reference is the likely slip.
The name rides beside the ordinal so a plan can be checked against the schema at that position rather than trusting it — #135's class, caught at plan time here.
The name the aggregator goes by, in a plan line and in DataFusion's own state field names (`avg(x)[count]`), which is how our state columns find their types.
The name the key project gave that column, which is the probe's: this join is its only reader, and two names for one column is how the two sides drift.
The name, the node's own fields and its layout — everything both goldens carry.
The names are the query's and the types are the aggregate's: renaming a column is not retyping it.
The next batch, or `None` where the mapping gave this lane nothing more.
The next batch, or `None` where the mapping gave this lane nothing more.
The node `depth` steps below the root, following first children — which is how a case names the one node it is about without the plan builders returning handles.
The node and the lane are what a message can name that a backend cannot.
The node at `seq`, by the post-order the C++ indexes with — children in `node_children` order, then the node.
The node itself or anything under it — a sort declares its own order, and a scatter its own hash.
The node kind on the wire.
The node line is the plan golden's minus the declared schema — a plan fact, recorded once beside the plan — plus the two totals both mode families carry.
The node line: the name, the node's own fields, its layout and its declared schema.
The node's children-first position, which is what a recipe is addressed by.
The node's declared row out of what the finish emitted, or `None` where the finish already emits it.
The node's declared row, out of an anti join that emitted build columns only: each column the projection keeps, in its order — a build one read where the anti join left it, a probe one as a typed NULL.
The node's own schema, which is not always the one DataFusion answers with: a partial aggregate names its state columns after the accumulators it ran, and this mode names them in the schema every reference above resolves against.
The node's own type, and `None` for the two joins that have none — cross and nested-loop.
The node's post-order position, which is what the recipe plan is indexed by — the same order the memory section numbers in, and not a `Seq`.
The node's projection over the finish's own output: column refs and nothing else.
The node-local checks read a node's keys against its input; this one reads the claim it makes about its own output, which nothing else looks at.
The nodes that change which lane a batch is in.
The nodes that hold a lane's batches back: coalesce-all, accumulate-and-sort, and the mid-plan limit — which is an accumulator by category and holds nothing at all.
The nodes whose row interval was satisfied, in index order — empty on a run that drained.
The number is the whole content of an address, which is why a call carries nothing else about the node it runs.
The offsets for one node's slots: the unconsumed ones first, then stubs.
The one a map arm runs; a `CudfProject` with no role of its own.
The one aggregate whose merge is not a per-column reduction: combining two partials needs the count-weighted mean and the cross term.
The one case that runs under a runtime with threads to spare, because every other test here is a plain `#[test]` where DataFusion never spawns.
The one downcast point.
The one exception in the decomposition table: a count merges by SUM, because what arrives is already counts.
The one file the groups below are numbered in.
The one file the row-group indices below are numbered in — taken from the metadata read that produced them, never assembled beside it.
The one input the narrowing cast exists for.
The one input this call exists for is the one it would silently swallow.
The one literal the wire cannot carry, in a node with an input — so the walk has something to lose. #168 is the corpus case (`mixed-join`'s residual adds an interval to a column), and the failure mode it guards is structural: a placeholder that dropped the inputs its node had taken would leave them unreachable, and the tree would then be shorter than the numbering.
The one node of the partition-accumulator category: every lane's sorted run merged into one at the last lane's done, and nothing where no lane sent anything — a merge of no runs is the collapse of nothing under another name, and the device refuses that (#173).
The one node of the partition-accumulator category: every lane's sorted stream merged into one at the last lane's done.
The one place the sentence itself is pinned, so the human-facing half cannot drift away from the field beside it.
The one place where naming the merge separately from the init is the difference between a right and a wrong answer.
The one thing a per-batch answer cannot get right on its own:
The operator is built at construction and the batch is what changes per call, so what a call does is hand [`execute_single_node`] one node and the rows it runs over.
The oracle comparison the injector exists for needs real operators and belongs to T14.
The order every branch agrees on, or none.
The ordinal is authoritative and a name is printed beside it, so `name@ordinal` is the form everywhere — and a literal is quoted on the same rule as a name, since a rendered value can hold the comma the list is cut on.
The ordinal is authoritative and the name is the declared schema's at that position, so a name disagreeing with its ordinal is visible on the line.
The ordinal is structural: build width 1, so the probe's key is at 1.
The other node that finalizes: the translation hands its finalize to the init itself where one batch in one lane is already the whole of every group, so no merge exists to carry it.
The outer join's build side is itself a join, and a build subtree is never held — which is what keeps the whole arrangement from deadlocking.
The outer join's probe side holds an inner join: the outer build drains, the outer hold lifts, and only then does the inner one get to run at all.
The output holds the build side alone, at its own ordinals.
The output is the join's own rows in the order its probe batches arrive.
The output names are DataFusion's, so a finalized column lands where the plan above it expects to read it.
The output ordinal is neither side's: the projection drops a build column, so the build key lands at 0 having been 1 in the crossed table.
The output schema is the finalized one where this node finalizes, so a consumer of the state reads this instead.
The pad project would read that ordinal as a probe column and write a typed NULL where the mark belongs, which is why the narrowing project walks what the finish emitted rather than build-plus-probe.
The parquet reader answers in the file's own arrow types, and DataFusion's scan does not: a string column reaches the rest of the plan as a view type, and every node above was planned against that.
The part of it that cannot be an overestimate — a build side is its input's rows, where an aggregate's state rests on a cardinality estimate.
The partitioner decided at plan time which row groups each of a lane's batches reads, and this reads exactly those — so a lane's batch count and its rows are the plan's rather than a reader's chunking.
The parts that are not zero, in the units they are declared in — `90 days` where the struct form spends sixty characters saying the same thing.
The pass-through moves every height and every rank without touching a batch; the rebatcher also changes the shape batches arrive in, which is the property the prototype injects #139's node for — and this mode has one today, since a coalesce is N-to-1.
The peak is an observation taken elsewhere and is not what is checked, so it can exceed a budget that never tripped — see the Memory accounting section of the task spec.
The peak of this plan is one scan batch in flight, so the scan is where it is reached: the filter is 1:1 and releases its input as it produces.
The per-batch half of the decomposition, without deciding which accumulator goes above it: one lane's stream sorted is an accumulate-and-sort, an N-into-1 is a merge, and only the parent knows which it needs.
The per-batch sort's fetch, over a run long enough to leave the insertion sort behind.
The per-batch sort:
The per-call join disappears: a probe call is only the key project, and the build side is not touched until the finish consumes it.
The per-lane half exists to shrink what crosses the shuffle; where the lanes stay put there is nothing for it to do that the finishing merge does not.
The per-lane merge shrinks what crosses the shuffle; the coalesce below the scatter is what makes it one call rather than one per merged batch.
The phase as data and the sentence as words: a caller branches on the first, a person reads the second, and both are pinned.
The pick: the runnable node of smallest height, ties leftmost.
The plain case, and the one every other reading is measured against.
The plan above in pre-order, which is the numbering every assertion here is written in.
The plan golden carries the declared schema per node; an execution golden does not, since what it records is what ran.
The plan nodes, grouped by family; the downcast registry over them is at the end of this file.
The plan the finding is about: everything the source produced, held by one node until it emits a copy of it.
The plan under `root`, one line per node.
The plan's declared width times the rows, plus what the var-length columns actually hold — the same formula the device is priced by, and by every other component in the tree.
The plan-time memory model: what each node holds, and how large a source's batches may be for the whole plan to fit a budget.
The planner end to end, through `plan_batch_partitioned` rather than through a rule called directly: the two tests below are what pins that validation is wired into it at all — every other test here calls the translator or a node's own check.
The planner inputs a mode fixes: how many lanes to aim for, whether a lane holds more than one batch, the budget the estimator divides, and the byte count below which a source stops being worth splitting.
The planner makes a refusing shape's probe a single batch, and this is that batch: what the matrix calls the legacy call is one call over the whole of it.
The planner refuses on a constant only where the constant cannot be an overestimate.
The planner's canonical form, checked on the way in: a mock plan meets the same refusal a planned one would, rather than the driver quietly running a tree the planner does not emit.
The planner's half of [`Batching`]:
The plans are constructed rather than planned, so a test names the shape it means; the oracle is written down rather than computed by anything under test.
The pointer is BORROWED, as everywhere on this path — the session outlives every executor drawn from it, and the handles they hand each other.
The portable half of the prototype's stress surface: the knobs that assert flow rather than answers.
The post-check runs after the emitting call's state has been forgotten, so it reads 3200 where the instant before held 6400 — the peak is honest and the check never sees it.
The predicate is the join:
The predicate rewritten as the prototype states it: rescan every node, walk both hold relations from scratch.
The predicate the prototype evaluates per step — every node, every lane, and both hold chains to the root — is O(nodes × lanes × depth) in front of work that is O(lanes) executor calls.
The probe arrives in two batches on purpose — a build row unmatched in the first and matched in the second is the whole reason the finishing types keep the probe keys and answer at done rather than per call.
The probe keys a finishing type keeps until its finish pass runs (#136).
The probe keys a finishing type keeps until its finish pass runs (#136).
The probe keys each batch contributed, held until the finish concatenates them.
The probe keys kept per batch, which the finish pass joins against (#136).
The probe keys this batch contributes to the accumulation (#136).
The probe keys this batch contributes to the accumulation (#136).
The probe keys this batch contributes, under the names they carry in the accumulation.
The probe side had already produced: those batches have no reader now.
The probe side is always the right child, which is the orientation the schedule's leftmost tie-break turns into "the build subtree drains first".
The probe's key columns in the order the join hashes them, keeping the probe's own names — the shape `recipe::join::key_project` writes, and the schema its output is priced by.
The probe-side semi family: membership in a complete build side is a per-row question, so every row leaves with its own batch and there is no finish at all.
The project that carries it emits the keys first, and `recipe::aggregate_writer::finalize_project` is the one place that rule lives.
The projection indexes the file schema, and a column chunk sits at the same position: every table this engine reads is flat.
The projection is over the joined table, so the padding is read there and then narrowed.
The property `assert_drained` enforces, stated: every lane of the root ended, and no queue in the tree still holds a batch.
The property the numbering exists for: a hold is expressed as a range, so a subtree has to be contiguous.
The query is over, and the site is what a message can name that a backend cannot.
The question a streamed probe could not answer, in the calls the recipe names: the keys concatenated, the finish join against the build side, and the pad where the node's output is the joined schema.
The question a streamed probe could not answer: which build rows nothing matched.
The reason, its ticket, and the seq the node would have taken — the three things a reader of the golden line needs, and the assertion that keeps the Err arm real: it would go quietly green the day the writer learns intervals, having stopped testing anything, so the message is checked rather than merely the failure.
The reasons are `llm-wiki/architecture.md`, under Execution.
The reasons behind each shape are in `llm-wiki/architecture.md`, under Execution.
The reasons behind each shape are in `llm-wiki/architecture.md`.
The rebasing the layer does at every inserted node is what makes this the likely slip: the ordinal stays valid and starts reading a different column.
The refused call is the accumulator's `mark_done_and_fetch`, and a call records itself only after it returns — so the last thing in the trace is what ran before it, the source reporting itself exhausted, and never the MarkDone.
The registry is the mapping, so a node kind is named in one place; a node outside it — a hand-built one under test — says its own name rather than reaching a registry it is not in.
The registry without its panic, for the generic validation pass: a node it does not know is a hand-built one under test, which declares none of the parameters that pass reads.
The release is skipped because C++ has erased the registry entry: releasing again would use a dead handle.
The resident total [`ResidentAccountant`] checks, kept incrementally:
The residual, rebuilt against the intermediate schema its column map names — the same reconstruction the wire's own reader makes, since the map is the same map.
The rest emit their input's columns, which `types_across_the_edge` compares field for field — a stronger statement than a count.
The rest is what a reader of a golden needs and cannot derive: the layout a node declares, every `fetch` that trims rows, the loader's mapping verbatim, and the declared schema, without which an explicit cast's target means nothing.
The retired pre-tier label spelling must NOT resolve.
The rewired method, and the whole of what this type is for.
The rollup's second set substitutes NULL for key 0, so the rows it produces are no longer in the lane the hash on that column put them in — and a finalizing merge that believed the claim would answer per lane for a group spread over all of them.
The root not being done is the same failure seen from the other end: a node that never learned its input had ended never emitted, and no queue holds the evidence.
The root, with the limit lowering rule applied: a root-adjacent limit is not a node at all — its interval becomes the unload's, because a limit over a stream about to leave the device is a statement about which rows are worth moving.
The round trip a CPU executor makes:
The routing a node declares, which is a property of the node rather than of a backend — every backend would compute the same thing, so it is read off the node by whoever routes.
The routing a node declares, which is a property of the node rather than of a backend — every backend would compute the same thing, so it is read off the node by whoever routes.
The row after the last one wanted, counting from the start of this node's stream.
The row groups per batch this lane still owes, front first.
The row groups per batch this lane still owes, front first.
The row groups the mapping addresses, with their rows and their parquet bytes over the projected columns — the only real numbers a plan-time model has, and what lets the estimator price the batches this mapping actually produces rather than the ones a budget would have afforded.
The row range arrives per call because a root-adjacent limit counts across lanes and only the driver holds that count.
The row range is a call argument because the count a root-adjacent limit derives from is cross-lane, and an unload instance is per lane — only the driver holds that count.
The row-group → (partition, batch) mapping: one pure policy, computed once at plan time, whose output everything else consumes verbatim — `GpuLoadParquet` stores it, the plan golden renders it as `partition_groups=[...]`, the loader executes it, validation checks the declared lane count against it.
The rows a limit names depend on the order they arrive in, so this shape reads as ordered and is not: the prefix is whatever the batch boundaries made it.
The rows in the types the node declares.
The rows of `batch` that belong to each of `lanes` lanes, in row order.
The rows of a batch this range actually names, as `(offset, length)`.
The rows of a batch this range actually names, as `(offset, length)`.
The rule for accumulators that are not limits.
The rule is the interval's, not the unload's: without one there is no prefix to be wrong about, and an unordered stream is unaffected either way.
The rule the planner applies, applied to a tree that did not come from it: a hand-built plan meets the same refusal a planned one would, and there is one statement of it rather than two.
The same call where the output count is a plan value: a scatter's N lanes.
The same columns under the names the node declares.
The same depth the C++ verifier allows (`NodeSession`): a recipe plan is a chain, one node per seq, so it is as deep as it is long — where a legacy plan is a tree and never near the default 64.
The same for a merge, and the reason is not only the device: a grouped merge over no arrivals owes no groups.
The same knobs over a plan rather than a query, for the shapes no sql reaches.
The same reconstruction serves the merge, where `merge_m2` is one call against three state columns and the wire spells it as the SQL aggregate plus `Merge`.
The same rendering with the column form supplied, because a join filter's ordinals index a table of the filter's own that appears on no line.
The same stub over a schema already built — a merge's input is the state its child emitted, which is a `Schema` with annotations rather than a column list.
The same sum a column later: a merge reads the state its child emitted, where column 1 is that sum rather than the value it came from.
The same total rows over one lane and over four.
The same under a fetch, which is the half that used to keep a heap: the rows kept are the ones whose keys win, and which of four tied rows was kept does not change between runs.
The scan below still reads what parquet holds: the widening is the project's.
The scan is not here: it opens its file when it is built, so it is proved in `source.rs`, over a parquet that exists.
The scan the driver overrides per batch.
The scan under a query, which is what carries the projection and the pruning predicate this file reads.
The scan's projections carry the file ordinal each column came from.
The scatter drops an output holding no rows before it reaches a queue, so the count is what flowed rather than what was returned.
The scatter is the one call whose output count is a plan value rather than one, and the count is the contract: a driver reads output `p` as lane `p`'s, so a lane the hash sent nothing to still comes back as a handle to an empty table.
The schedule and the two holds, asserted on calls rather than on rows.
The schemas a node declares it consumes, in child order.
The second and third aggregators of a triple add nothing: the SQL name covers all three columns.
The second pass: each source gets the batch size the estimator solved for it, rather than the one number the first pass had to assume.
The second spends what is left.
The semi family emits one side only.
The seq a call addresses, once the call is what this node's recipe should hold.
The seq a failing payload would have taken, appended to the reason rather than wrapped around it: a second `PlanError` around the first prints its prefix twice.
The seqs this node addresses, in the order it emits them.
The serialized recipe plan: what `peacock_executor_begin_plan` is given, and what every seq in every recipe indexes into.
The session is gone with it, so the query is over.
The session is this executor's, as everywhere on this path: a batch carries the pointer only so that dropping it can release its handle.
The session is this executor's, not the batch's: a batch carries the pointer so that dropping it can release its handle, and every batch reaching a node was drawn from the session the node was built against.
The session pointer is BORROWED, as everywhere on the GPU path: the session outlives every executor drawn from it, and the handles it hands back.
The shape a join minted before it read its children: lanes hashed on a column nothing ever scattered by, which a co-partitioned join above would then trust.
The shape every join with no finish takes: one call per probe batch and nothing at done.
The shape of an avg's finalize, which is the deepest thing the planner builds: a divide of two casts, under a CASE, under a NOT.
The shape that would owe a row — a global aggregate's identity row, count 0 rather than no row — has one lane and no scatter above it, so its lane is never the empty one.
The share these sources can afford is far above what they hold, so each target is the source's own size: a batch is never larger than what there is to read.
The shuffle, as both halves: nothing to merge below one lane, and a lane that arrived as one batch per lane is worth re-coalescing so the scatter is one call rather than one per lane's batch.
The sibling memory golden: the same tree, carrying what the estimator derived rather than what the planner decided.
The sides whose rows this join type emits unpadded.
The single-node shortcut: state and finalize on one node, which is two operators here and two calls on the device.
The skew an emitter fills its lanes by, which is what the shuffle cases rest on: the named lane takes every row and the rest come back empty rather than absent.
The slice reports no stats, so the batch is priced from the rows asked for: a range that clamps is one this node computed against the batch it holds.
The small-table rule: a source under the threshold plans one lane whatever the target, and the nodes a one-lane region does not need are then never emitted.
The sort an accumulating node runs per batch before its merge: same node, and the `fetch` rides the merge rather than the per-batch sorts, which must keep every row a later batch could outrank.
The source's consuming step removes a fifth — the driver's own exhaustion flag.
The split into init / merge / finalize is this table, and adding an aggregate is a row here rather than an arm in C++.
The split is not DataFusion's `Single` mode: this mode finalizes in a project so that both engines evaluate the one finalize expression, and a `Single` here would be a second implementation of it.
The state columns are relabelled by position, so a declared state that is not the one DataFusion's accumulators produce would rename a column rather than fail.
The state columns are relabelled positionally, so what DataFusion produces has to be the shape the node declared.
The state field DataFusion declared for one of our aggregators.
The state names are ours — the golden and every later reference read them — and the types are DataFusion's.
The state this node merges into, before any finalize of its own — see [`GpuAggregate::intermediate`].
The state's key columns lead it, so a group position is a position in it — the same rule the recipe writer's group names follow, since the two sides have to agree on which column a key landed in.
The state's key columns lead it, so a group position is a position in it.
The straddling batch: the driver hands a range because the rows wanted are a window inside this batch, and shipping the batch to trim it afterwards is what the range exists to avoid.
The structural rules here judge what a node says about its *own* output — the ordinals in its layout and its annotations — which no child can be blamed for and which no node-local check looks at.
The successor reports for the same slot: what the build side became is this instance's residency now.
The surface has no copy symbol, so a recipe naming this is one an executor refuses until [#145](../../../../llm-wiki/tickets.md#t145) — see #152.
The symbol follows from the kind: only a scan takes the row-group override, and everything else is the generic entry point.
The table a scan reads, named after the parquet file rather than declared anywhere:
The target batch size goes; batching is this mode's own concern (#139).
The test's own input, so the sizes below are data rather than expectations.
The three aggregators a stddev decomposes into, each over the same value expression — which is what makes them one SQL aggregate on the far side.
The three refused shapes are refusals of a defect or a missing cuDF variant, not of this mode: an outer join's residual filter is applied after the outer gather and drops the padded rows (#153), and no swapped `mixed_*` variant exists for the right-handed semi family.
The three refused shapes are refusals of a defect or a missing cuDF variant, not of this mode: an outer join's residual filter is applied after the outer gather and drops the padded rows (#153), and no swapped `mixed_*` variant exists for the right-handed semi family.
The three that route rather than compute: not one call between them.
The three things a limit does to a batch, in one stream: the first is wholly before the interval, the second straddles its start, the third is wholly inside, the fourth straddles its end and the fifth is wholly past it.
The three types that preserve unmatched probe rows owe their probe side, and making it takes a call over a build table that does not exist (#175).
The three where the executor's `null_equality` is not the plan's to choose.
The threshold a batch aggregate compacts at, until the driver derives one from the budget the way the loader's batch size is derived (#142).
The threshold a batch aggregate compacts at, until the driver derives one from the budget the way the loader's batch size is derived (#142).
The threshold doubles when a compaction fails to shrink what it folded — see the CPU backend's copy of this rule, which is the same rule and the same reason.
The ticket rides the message because it is what a reader of a golden gets: the wire's `ScalarValue` has no interval, and one corpus residual adds one to a column, so folding never reaches it (#168).
The to-the-end sentinel is the case the clamp is written around: subtracting the offset from the row count rather than adding it to the length is what keeps `u64::MAX` from wrapping.
The topmost node the predicate accepts, parents before children.
The traits are synchronous and DataFusion's operator API is not, so each call blocks a thread on one node's stream.
The tree as `Parent(Child, Child)` — enough to assert which nodes were emitted and where, without standing in for the plan renderer.
The tree as plain numbers.
The tree as the planner shapes it: whatever is given, under an unload.
The tree children-first, which is what `attach_recipes` numbers by.
The tree in canonical post-order — children left to right, then the node — which is the order the goldens render and the order every per-node figure is indexed by.
The tree indexed once — heights, pre-order numbering, lane counts and slot ranges.
The tree indexed once: heights, pre-order numbering, lane counts, and the ranges every hold is expressed over.
The tree is heterogeneous and planning is not hot, so this is the one place trait objects are used; everything on the per-batch path is static (see `executor/mod.rs`).
The tree is walked through the driver's own [`PlanIndex`], not through a second walk of the same shape: the report is addressed by that index, and two walks agreeing is a coincidence to be avoided rather than relied on.
The tree is walked through the index the report is addressed by, so nothing here re-derives a child list.
The tree's shape does not move between them: lane counts come from row counts and pushed-down limits, never from the batch size.
The trivial kinds are not tested here — the plan goldens run them over every corpus query, which is more coverage than a hand-built node would be.
The twin of C++'s `clamp_row_range` (`node_session.cpp`), which the export and the slice share so that the two cannot disagree — this is the same rule for the backend that never crosses the ABI, and the two answering differently would be a divergence no test of either one alone could see.
The twin of C++'s `clamp_row_range` (`node_session.cpp`), which the export and the slice share so that the two cannot disagree — this is the same rule for the backend that never crosses the ABI, and the two answering differently would be a divergence no test of either one alone could see.
The two are written in different places — the schema comes from DataFusion, the parameters from this layer's own rebasing — and a node emitting a different number of columns from the one it declares shifts every ordinal above it.
The two batches that straddle an interval's ends; a batch wholly inside is forwarded untouched and one wholly outside is released uncalled.
The two drivers, the schedule they follow, and the accounting they fail a query on.
The two halves answer different questions.
The two halves are separate failures.
The two overlap only if the positions were derived against a different column order, and a merge reading a key as state would merge the thing it groups by.
The two passes map to each other by that order alone, so a second pass reaching a source the first did not would plan its tail at the seed size — a different plan from the one the estimator priced, arrived at silently.
The two rules an aggregate body states about its own state columns: which aggregator owns which of them, and what the finalize project emits.
The type decides what it owes, and the rule is the one the CPU reads too.
The unload holds nothing and its transient is the rows crossing out, which on this backend is a slice of a batch already here.
The unload is the crossing: these bytes are on the host now, so they leave the accounted set as they are handed to the caller.
The unload's row range is not in here: it comes from a count across lanes that only the partitioned driver holds, so it is an argument of the call rather than part of the decision.
The value alone, without the type wrapper `ScalarValue`'s own Display prints for some variants.
The variant is what a call site reads: a coalesce that concatenates, a sort that orders the whole stream, a merge that folds state on a threshold, and a limit that forwards, slices or drops.
The variants are `mod.rs`'s; these are their two trait impls.
The vtable half of a trait-object pointer is not stable across casts, so only the data half is taken.
The walk that builds the recipe plan's bytes, and with them the seq numbers.
The whole sequence, from the aggregators the partial declares: init per batch, a per-lane merge where a lane holds several batches, the shuffle where the lanes must be re-landed by group key, and the merge that finishes it.
The whole stream, not each batch: batches that are each sorted and not sorted against each other is exactly the shape that reads as ordered and is not.
The width check, shown going red: a finalize list that does not account for every non-key output column is a plan that answers with a column missing, which is a wrong answer rather than an error unless this refuses.
The width matters here — the state columns are found by walking past the keys — so a case that plans two sets is what tells a right width from a wrong one.
The words the message uses.
Their ordinals are `0..k` and their names are the probe's, since the key project is what built that table — no residual and no projection, because the question is only which build rows matched.
There a merge is the SQL aggregate plus a mode; here DataFusion has no mode that reads state and emits state, so each of this mode's own merge aggregators is resolved on its own — and the one with no DataFusion aggregate behind it, the Welford triple, gets [`merge_m2`].
There is no key to re-land rows by, so DataFusion coalesces and the sequence merges into one lane rather than scattering into four.
There is one response to every backend failure — stop — so what these assert is that it stops, that the message says where, and that the accounting still reconciles on the way out.
They are the wire's channel for `avg`'s declared decimal type (read by `aggregate.cpp` at :216 and :711), but this mode never sends an `avg` to a device: decomposition splits it into sum and count, so the scale rides on the finalize divide's own `out_decimal_precision`, which `expr_writer` sets and `an_average_finalizes_to_the_digits_the_oracle_computes` proves against the oracle's digits.
This backend runs that decomposition rather than one whole join at done — the plan says the probe streams, and an executor holding it all would be a different plan wearing the same shape, and a poor oracle for the one the device runs.
This batch's contribution to the accumulation: its key columns and nothing else, which is what makes the concat at done cheap (#136).
This is the single source of truth for per-type widths.
This lane's build side finished with no batch — its scatter gave it no build rows.
This lane's build side finished with no batch, which a small table scattered over many lanes produces routinely.
This mode decides its own lane counts — a cross join or a small source can put one branch on a single lane — and branches that no longer agree have no shared distribution to interleave on.
This mode stacks two merges — per lane, then across lanes — so the lower one must emit the state the upper one reads.
This mode's [`Expr`] into the wire's `fb::Expr`.
This module is the vocabulary — plan nodes, the layout and schema they declare, and the executor contracts the drivers call.
This node's position in a children-first walk, which is what `RecipePlan` indexes by and what a backend hands to `executors_for`.
This one can only say what is in the buffer.
This term telescopes across batches — the sum over batches equals the value for the whole node — so it carries no per-batch overhead and is safe to accumulate.
Three batches on each of two lanes, failing at the first exec: what must not happen is the rest of the plan being driven anyway.
Three batching forms, one budget: what a loader holds is the largest batch the mapping produces, so per-row-group holds a row group where one-batch-per-lane holds the lane.
Three call sites hand a backend a post-order, and they read it from different places: a lane site carries one, and the forwarder wiring and the cross-lane build each take it off the index themselves.
Three counts, three categories, and they differ for reasons a reader cannot see from the lane count alone.
Three lanes of one four-row batch each under a fetch of five.
Three named forms rather than one number with special values: which one a mode uses is a statement about the mode, and a target of one byte reaching the same place would hide it.
Three of the join types hardcode `cudf::null_equality::EQUAL` whatever the plan's flag says (`cpp/src/operators/join.cpp`), so a NULL key matches a NULL key there.
Three of these four are properties a plan line has nowhere else to say.
Three rules keep the order it creates nodes in equal to the order `NodeSession::index_post_order` visits them, and each is stated where it is applied — [`Writer::take`], [`Writer::stub`] and [`Writer::reduce`].
Ties are broken partition-major — lane order, then arrival order inside a lane — which is what concatenating in lane order and sorting stably gives, and what a k-way merge over the same runs gives.
Today both end the query.
Treat any None variant as typed null.
True once no further row could change the answer — what `is_satisfied` asks.
True where every output row is built from a build row, so an empty build side is an empty answer and the lane can end without a call.
True where every output row is built from a build row, so an empty build side is an empty answer and the lane can end without a call.
True where nothing needs a finish, and also where the probe cannot stream — the planner makes that probe a single batch, and one call over the whole of it is the plain join node the wire has always carried.
True while this lane of a join has yet to leave its build phase.
Twenty of the forty rows that reached the unload were before the interval began.
Two decimals of different scale meeting at a union: routing cannot retype, so the planner owes each branch a project that lands on the declared type (#41), and the declared type is DataFusion's coercion rather than one this mode derived.
Two engines wrong the same way is what the goldens cannot catch, so the rule is stated where it can break.
Two nodes emitting the same seq set differ by this and nothing else — a sort per batch and a sort at done are different plans.
Two numberings computed by two walks in two files — one at plan time, one at run time — agree by construction only until someone changes a walk, and nothing else compares them.
Two of the three forms are the plan already; only the budgeted one needs a number to start from, and the smallest the mapping can express is the one that assumes least.
Two of them take runtime bounds instead of a seq, which is the reason they exist: a frozen node cannot carry a number that is known only once the rows have been counted.
Two passes, because the two halves define each other — a batch size needs the tree it flows through, and the tree's mapping needs a batch size.
Two passes.
Two reasons, and both engines evaluate this expression: arrow refuses UInt64 - Int64 and Float64 / UInt64 outright rather than widening them, and a count below ddof would wrap rather than go negative in the count's own unsigned type — which is exactly the case the NULL arm is for.
Two recipes ask for one anyway, and both are refused by name rather than run against a dead handle: a streamed probe's second batch, whose build side the first call erased, and a Left or Full join's probe batch, which its key project and its join both read. #152 records them and #145 retires both.
Two shapes, and the difference is whether the finish's output has a probe half.
Two unloads: the inner one already moved its rows to the host, so the outer one is reading something that is no longer on the device.
Two-valued on purpose.
Types and literals are DataFusion's own — the coercions and the decimal precision and scale it derived are exactly what must not be re-derived (#55/#56/#63 are what re-deriving them costs).
Types are asserted on the tree rather than on the rendered plan.
Types are read back off DataFusion — a binary op's declared output type in particular, since cuDF's own fixed-point result scale differs from DataFusion's.
Types are the half no name check and no per-node byte count can see: both engines derive their bytes from this same declaration, so a column that changed type across an edge costs the same on either and surfaces only in the answer.
Types are the planner's own, so decimal precision and scale are not a second copy that can drift.
Types only: the names are what differ by design, and nullability is DataFusion's own, copied into the declaration when the plan was built.
Under by 100x and the query still runs: the enforcer's contract is the accounted peak against the budget, not that the model was right.
Under it goes one line holding what the node consumed and the size of every batch it emitted.
Under mocks "the same answer at every shape" degrades to "every row that entered was delivered", which is still worth asserting — a routing bug breaks it.
Under the call it belongs to, indented past the tree, so a long payload reads as this node's rather than as the next line of the plan.
Union and interleave: lane relabeling, and nothing else.
Union branches are planned independently, so one column can arrive as a different type per branch.
Unload is its own category because it is the one operator whose output is not `B::Batch`: this is where data leaves the device, and the type says so.
Unquoted, `order count@0 as order count` leaves a reader no way to see where the name ends.
Unsafe rather than safe casting, which is the whole point: arrow's safe cast turns a value that does not fit the declared precision into a NULL, and a NULL in a sum column is indistinguishable here from one the data had.
Until every lane has reported, the answer could still change: a lane yet to send its rows may hold the smallest of them.
Used by the tp8 partitioning step when there is no static predicate to prune by, so the scan-batch→partition map still covers every group.
Utf8View is a DataFusion 45+ optimizer rewrite of string literals; cuDF doesn't distinguish view vs. owned strings.
Validation before the model: the estimator walks the same tree and reads a node's declarations as facts, so a malformed tree meets a message naming the fix rather than an `expect` inside the walk.
Validation of a whole tree: each node against its children, then against itself.
Values are deduped/small; omitting them slightly undercounts but keeps the golden deterministic (no allocation-size dependency).
Var-length: only the offset buffer is structural; the content is accumulated separately (see `array_content_size`).
View layouts also carry an (rows+1)*4 offset-equivalent, mirroring the old formula.
View layouts: Σ value byte lengths.
Walking the projection rather than the build side is what keeps this the node's shape: the anti join emits every build column whatever the projection says.
We declare aggregators — `count`, `mean`, `m2`, `merge_m2` — and the wire declares SQL aggregates plus a mode, leaving the state layout to the executor.
What `executors_for` builds, and what the executors it built report holding.
What a call gives back.
What a call produced, priced by the schema the node declares: the ABI reports rows and varlen content, and the fixed width per row is the schema's.
What a finalize would produce.
What a finish over no keys owes is decided by this rather than read back off the call list: two different nodes publish a LeftAnti at done, and one of them owes padded rows.
What a golden's `--- memory ---` section renders: a figure per node in canonical post-order, and the batch size each source was given.
What a join does per call, built once.
What a mode asks of the partitioner.
What a node declares about its output columns: arrow types, plus the semantics a consumer can check.
What a node declares about its output: how many lanes, how rows were routed into them, what order they carry and whether a lane is one batch.
What a node holds whatever the batch size — `None` for a node whose residency scales with its input batch.
What a node is called, in a plan line and in the validation message that names it.
What a node runs.
What a plan is: the eighteen nodes, the vocabulary they are written in, and the rules a tree has to satisfy.
What a plan line and a validation message call this node.
What a plan node offers the driver and the validator.
What a recipe claims and what the buffer holds are checked against each other through this — see `read::check_seq_kinds`.
What a recipe is made of: the four ABI symbols, the legacy node kinds they address, the handles a call is passed, and when the driver makes it.
What a recipe-plan node carries, read back out of the bytes and written as text.
What a sort here does promise: the keys come out ordered, every row survives, and the same input answers the same way twice.
What a source holds in total, over the columns it projects.
What an accumulator answers with at done: one batch of everything it held, and nothing at all where nothing arrived.
What an executor is built from besides its node: the GPU's open session and the recipe plan its seqs address, the CPU's `TaskContext`.
What an executor on this backend is built from: the open session, and the recipes whose seqs address the plan that session was given.
What an executor was asked to do.
What arrives at a node while it holds its state: a join's probe batch, per lane.
What came back, as a list per column, so an expected result reads as the rows a person would write down.
What can go wrong while a plan runs, as against [`PlanError`], which is what can go wrong before it does.
What each category does.
What each lane is holding, for the accounting the driver sums.
What each lane is holding, for the accounting the driver sums.
What each node emitted and what it consumed — the two records the corpus goldens read.
What each source may spend, before its own amplification and size narrow it.
What every call reports as measured scratch.
What every executor models per input byte, before its own state.
What every node declares about its columns: the arrow types, and the annotations a merging or finalizing node reads.
What is asserted is everything a plan depends on: the count, that no row is lost or doubled, and that a key's lane does not depend on the batch it arrived in.
What is checked here is only that the instrument reads what it was set to — a few cases, not a suite.
What is not reused is the shape: a column reference is an ordinal into a child whose column order this engine decides, so every reference is rebased at each node the translation layer inserts.
What is reused is DataFusion's planning — the coercions, the decimal scales, the per-aggregate state schemas — and not its execution semantics, which is what annotating a tree with wrappers carries along by accident.
What it adds is `Executor` — the held bytes and the pre-call transient the accountant sums — which no earlier task could answer, because nothing was accounting yet.
What it adds to the pruning pass is the per-group rows and bytes the mapping needs.
What it buys is a measured figure to check the model against: an exec node's model is its input's size, and a project that widens its rows exceeds that.
What it does not promise is tie order — neither DataFusion's sort nor cuDF's is stable — so a plan that needs one asks for a key that decides it.
What it holds is what the emitting call has to build a copy of, so a model that says so refuses the call rather than letting the two live at once uncounted.
What it holds plus what arrives: a compaction reads both at once, and a merge that is about to fold the two is the transient the accountant has to have room for.
What it holds plus what arrives: a compaction reads both at once, and the merge about to fold them is the transient the accountant has to have room for.
What it owes is the join type's answer.
What makes an empty `underestimates` mean the model held: a backend measuring nothing produces the same empty list, and the two are indistinguishable without this.
What no arm of `select` may do is name a slot with nothing in it — which is exactly what a probe reading the build slot did, and what that bug did not turn red.
What one aggregate node's aggregates become in each position.
What one lane's input edges look like right now.
What one node does to the device, in call order.
What one read of a scan's metadata tells the planner: the row groups it will read, and whether each projected column has a NULL in any of them.
What planning and validation return.
What sits between a partial aggregate and the final one:
What sql asked for.
What that looks like from the outside is a LIMIT quietly returning short, so the arithmetic is checked here where it is done rather than absorbed where it is used.
What the accountant decides at the boundary, through a real run.
What the build-side semi family's finish emits, which is what a project above it indexes: the build side, and the boolean a mark join appends to it.
What the call produced is priced by: the key project's output is the keys, and everything else is the node's own row.
What the capability matrix says about one join mode: whether the probe side can stream batch by batch, and whether the lane owes a pass at done for what a streamed probe cannot know — which build rows matched at least once (#136).
What the driver hands a backend when it builds an executor set.
What the driver tests assert is calls — pull counts, queue bounds, release — so an operator here only has to produce the right number of batches of the right size at the right moment.
What the executor calls this aggregate, which is DataFusion's own name plus the `ddof` spelled into it:
What the executor calls this aggregate, which is DataFusion's own name plus the `ddof` spelled into it:
What the finish would have computed is known without it — nothing matched — so an anti join hands its build side up, which is the answer and the one this backend can express.
What the first pass assumes.
What the golden's `early_exit=` marker names, and the reason a lane can be short of what its plan called for: not a bool, because a reader of a smaller number needs to know which limit produced it.
What the golden's `early_exit=` marker names, so a reader of a smaller number knows which limit produced it.
What the index derives, against a plan whose shape is written down here.
What the node declares, out of an anti join that emitted build columns only: each kept column in the projection's order, a build one read from the anti join's output and a probe one as a typed NULL.
What the per-probe-batch join emits, which is not what the node is: a Left emits this batch's matches and waits for the finish, and a Full also emits the probe rows this batch had no match for — batch-local, because the build side was complete before the first call.
What the per-probe-batch join emits, which is not what the node is: a Left emits this batch's matches and waits for the finish, and a Full also emits the probe rows this batch had no match for — batch-local, because the build side was complete before the first call.
What the plan line already carries is not repeated: a golden that states a thing twice is one a reader stops checking.
What the plan promised its caller.
What the plan promised its caller.
What the plan's implementation modules share: the input a node declares it consumes, and the three checks more than one node makes.
What the previous call in this recipe returned.
What the returned set drives.
What the same plan peaks at when the accountant only watches.
What the source holds is what makes the rest of the line legible: it is the cap on the target, so a reader sees whether the target is the source or the budget talking, and it is what the small-table threshold compared to decide the lanes.
What the typing buys, over a state no corpus query produces: a group with fewer rows than its degrees of freedom has no dispersion to report and owes NULL.
What the walk from one source found, and what it was given for it.
What this aggregate is called.
What this mapping emits, which is the budget's target only where the budget is what cut the batches.
What this node was asked to do — the parameters that decide its answer, and nothing that is derived from its children or repeated by the layout.
What to call, for a lane `can_step` has already accepted.
Whatever this node did not consume is gathered here, so nothing it was handed becomes unreachable: an orphan is never indexed and every seq above it would shift.
When the driver makes a call.
Where a call's input comes from.
Where a declaration must have come from.
Where a filtered cell's rows come out, which is the whole difference the residual makes: a shape that still streams answers per call, one that dropped to a single call answers there too but from one batch, and the finish pass is gone in both.
Where a limit sits is what decides whether it is worth having, and this is the case that says so.
Where a node's lane keeps its accounting slot.
Where an accumulator emits, which decides every queue-depth assertion made against it.
Where data leaves the device on the GPU path, and a slice on this one.
Where it finalizes, the finalize rides a project of its own — ours, so both engines evaluate the same expression.
Where one column of a join filter's own table comes from.
Where one key repeats the state stays a row wide, the threshold never moves, and an arrival compacts whenever the pending arrivals fill it.
Where one side's column lands in the output: into the emitted table first — which is the crossed pair, or the one side a semi form emits — and then through the projection, where a dropped column has no output ordinal at all.
Where the analysis cannot decide, a column is possibly-NULL.
Where the answer alone cannot say it: a row lost between two nodes and a row invented at a third return the same count.
Where the backend fails, if it does.
Where the budget is what binds, the target lands on the grid — powers of two, so an estimate that drifts slightly does not regenerate every golden.
Where the data leaves the device: one export per handle, over the row range the driver supplies.
Where the estimated constants alone exhaust the budget, batches are sized against the ones we know and the accountant owns the rest.
Where the executor comes from, so `run` can build one on first use without the driver deciding when that is.
Where the inequality actually is.
Where the keys are disjoint a compaction shrinks nothing, so the threshold moves to twice what it left and the compactions land at geometrically growing sizes.
Where this node's accounting slots start: one per lane when it is lane-scoped, one for the node otherwise.
Whether a lane whose build side produced no batch owes no rows at all.
Whether a lane whose build side produced no batch owes no rows at all.
Whether a lane whose build side produced no batch owes no rows — the join type's own answer, read once where the node is in hand.
Whether a lane with no build batch owes its probe side rather than nothing — the three join types that preserve unmatched probe rows, which no executor can answer without a build table.
Whether a node's row count is a fact rather than an estimate.
Whether a probe call reads the build side at all.
Whether a probe call reads the build side at all.
Whether an accumulator prices the output it is about to build.
Whether the join's output carries a column from each side.
Whether the join's output carries a column from each side.
Whether the produced type is the declared one with the precision a decimal sum gains per merge — the one mismatch [`declared_as`] casts away.
Whether the recipes section prints what each call passes the executor, or only which kernel it addresses.
Whether the whole join is one call: no probe keys kept and no finish pass.
Whether this aggregate has group keys.
Whether this input is the build side, handed over or copied.
Whether this join keeps probe keys and answers at done, rather than being one call and nothing else.
Whether this lane can make progress.
Whether this lane of this node can make progress.
Whether this names the whole of a batch that size — in which case the call needs no range at all, and the trace should not read as a trimmed one.
Whether this node's own interval was satisfied.
Whether this one is a folded triple, which is what makes its state three columns wide rather than one.
Which batch sits in which partition is the whole content.
Which call the backend fails at.
Which call this lane's input state calls for.
Which check refused the call.
Which columns a join type emits, before any projection of its own.
Which columns can be NULL, and the one join shape that has to be refused because of it.
Which executor instance a figure belongs to.
Which executor trait drives a node.
Which input slot the call consumes, in the node's own child order — so a probe reads the right child and a build the left.
Which is why the coverage check comes first: a plan shorter than the tree renders its tail as `none` too, and that reads as a forwarder rather than as the gap it is.
Which lane a key lands in is not asserted here — that is the murmur3 rule, and the live conformance gate is what ties it to the device.
Which lane a row belongs to, Spark-murmur3 — the one rule both CPU paths and the GPU kernel answer with.
Which lane each key landed in, by reading the lanes back.
Which limits were satisfied, by name and post-order address — the address because that is what both engines number a node by, and the name because nobody reads an address.
Which lowering a limit got is a question about position, and a node cannot see what is above it: root-adjacent means the interval belongs to `GpuUnload`, so a limit node whose only consumer is the sink is a tree the planner does not emit.
Which node runs next: smallest height among the runnable, ties leftmost.
Which of a join's two projects a seq is.
Which of the four shapes a join takes is `nodes::join::capability` — the same function the planner and the estimator read, so the recipe cannot claim a pass they did not plan for.
Which of two rows tied on the keys comes first is not promised — a k-way merge over unstable per-lane sorts has no order to preserve.
Which parts are emitted is decided here rather than by the node kinds alone, because the same DataFusion pair becomes a different tree at one lane and at four.
Which rows an interval names depends on the order they arrive in, so a limit under an order is only the top n where the whole stream carries it.
Which rows of the next batch are wanted, or `None` to release it uncalled.
Which side each column of a join filter's own table came from.
Which side of the decomposition a node runs: state built from raw values, or state merged from state.
Which trait drives a node, with the executor stored inline — the match compiles to a jump.
Which variable reaches the message is a live choice in the driver — the emitter passes a literal 0 where the accumulator passes its lane — and a message hard-coded to lane 0 would pass every other case here.
Whole stream ordered, not merely each batch — what a top-N after a sort needs.
Whole-text asserts rather than substring ones: what the renderer is for is a file a comparator reads line for line, and a test that checks a line is there does not check that nothing else is.
Why a call failed: a message and no kind, because there is one response to all of them.
Widths are real, from the declared schemas.
With a single state column there is nothing to mismatch; beyond that the aggregator's tag is what names it (`avg(x)[count]`), and a tag with no field is a drift this must not paper over.
With no key to co-locate on, both sides are one lane — the probe as much as the build, though only the build is also one batch.
With one batch per lane there is nothing for a batch-size threshold to size, so the rule that drops a source to one lane has nothing to act on either.
With one batch per lane there is nothing for a size threshold to size, so the rule that drops a source to one lane has nothing to act on either.
With one, the ordinals at or above the build width are the probe's — here `v` alone.
Without a projection every probe column is kept, so both are padded.
Without the check the loss is silent, since the batches are simply dropped with the driver.
Write one expression, children first, as FlatBuffers requires.
Written out rather than defaulted so a field added to the table has to be answered here.
Zero and two are what a join's build side must never do.
Zero at the end of any correct run: a batch was held and never released otherwise.
Zero everywhere on a run that drained, and what closes `consumed + abandoned == the child's emitted` into an equality on every run.
Zero for every other category.
Zero is a join that needs no finish.
Zero rows is not zero bytes: an empty lane still owes a typed batch.
[2, 4, 6] and [10] merge to a count of 4, a mean of 5.5 and an m2 of 35 — which is the sum of squared deviations from 5.5, computed by hand and not by another accumulator.
[`CpuBatch`](super::CpuBatch)'s side of [`Batch`](super::Batch).
[`GpuBatch::consume`] is that boundary, and the only place the release is skipped.
[`GpuBatch`] and the GPU backend exist only where cuDF is linked, so their declarations carry the cfg rather than only their modules.
[`GpuBatch`](super::GpuBatch)'s trait impls, and the one way out of it that skips the release.
[`RowInterval`](super::RowInterval)'s range, which is three statements rather than one.
[`RowRange`](super::RowRange)'s clamp, which C++'s `clamp_row_range` is the twin of.
[`execute_single_node`] replaces the children with the batches it is handed, so what is stored is the operator and its expressions, never a source.
[`expr_translate`]: super::expr_translate
[`partitioned`] owns the tree, the queues, the schedule and the three cross-lane categories; [`single_partition`] is one lane of one lane-scoped node, deciding which call that lane's input state calls for and making exactly one.
[label, v]: one column from each side, and the build key dropped — so a pad that emitted the build side whole would produce three columns, not two.
`(kind, when)` per call, which is the pair the mapping table states.
`-1` is unlimited, which is how the frozen nodes spell `None`.
`Avg` is never one — decomposing it is the point — and `MergeM2` is never an `AggFunc`.
`Budgeted` has no number until the estimator solves for one, which is why it is a separate word.
`ByHash` is Spark murmur3 seed 42 — the only routing `GpuEmitPartitions` has.
`Combined` exists only because `merge_m2` is not a per-column reduction: it needs the count-weighted mean and the cross term.
`CpuBackend`: the seven executor types this backend names, and the memory each reports.
`CudfScan` is the only leaf the schema has — every other table carries `input`, `left`/`right` or `inputs`.
`Err` where a node's payload cannot be written — an expression the wire has no shape for (#168) — because a plan missing one node's arguments is not a plan the C++ can be handed, and the alternative is a buffer whose every later seq has to be checked against a list to be trusted.
`Err` where a node's payload cannot be written — an expression the wire has no shape for (#168) — because a plan missing one node's arguments is not a plan the C++ can be handed, and the alternative is a buffer whose every later seq has to be checked against a list to be trusted.
`GpuAggregate` runs per batch; `GpuAggregateBatches` merges pre-aggregated batches and emits at done.
`GpuBackend`: the seven executor types this backend names, and the memory each reports.
`GpuEmitPartitions` arrives with the shuffle shapes; what a one-lane plan needs is the merge.
`GpuEmitPartitions` on a device: the repartition arm, one call per batch, N handles out.
`GpuEmitPartitions` on the CPU: one batch in, N out, some of them empty.
`GpuEmitPartitions`: what a scatter owes its driver, and what a join needs of it.
`GpuLoadParquet` on a device: one `execute_scan_rowgroups` per batch, over the row groups the mapping gave this lane.
`GpuLoadParquet` on the CPU: one call per batch, reading the row groups the mapping gave this lane.
`GpuLoadParquet`: the one source node.
`GpuMergePartitions`, `GpuUnion` and `GpuInterleave` are one driver arm over three mappings.
`GpuUnload`: the boundary crossing, and the only node whose output is a CPU batch.
`LocalLimitExec` has no skip.
`Name: field, field` — the name alone where a node has no fields at all.
`None` => no pruning applicable (read all groups): no predicate, not exactly one file, unreadable metadata, or the predicate prunes nothing.
`None` accounts and reports without ever tripping.
`None` ends the run.
`None` for a sink, which structurally has neither.
`None` for the multi-file / unreadable cases (the caller then leaves the scan single-partition).
`None` for the two symbols whose arguments are runtime row counts.
`None` is a pure offset: no prefix determines the answer, so it never satisfies.
`None` is an uninstrumented run.
`None` is every column of it — a `CrossJoinExec` has no projection, and a predicate-free nested-loop join that lands here may.
`None` is the build-side semi family, whose probe call is only the key project.
`None` is the build-side semi family, whose probe call is only the key project.
`None` once a call has consumed it, which is the last probe call for a join that hands it over and the finish call for one that does not.
`None` where that node makes no ABI call at all, which is what a forwarder does.
`Ok` means the lane owes nothing and ends here; an `Err` names a type whose answer is its probe side, which needs a call over a build table that does not exist.
`Partial` and `Merge` on the wire — never `Final`, which also finalizes, and in this mode a finalize is a project of its own.
`Partial` builds state from raw values and `Merge` merges state into state.
`ProbingJoin` is absent because it comes from `set_build`, not from the backend.
`ProjectionMask::roots` is a SET: the reader emits the columns in file order whatever order they are named in, so a projection that is not ascending would hand this lane its columns permuted against the schema the node declares — and the device, which sends the same list to cuDF, would permute them identically, so the goldens would agree and be wrong together.
`ProjectionMask::roots` is a set, so a descending projection would come back permuted against the declared schema — on this backend and, identically, on the device, which sends the same list to cuDF.
`SELECT count(*) FROM (SELECT ...
`Some` only where a limit's interval landed — see the limit lowering rule.
`Sqrt` has no DataFusion unary, so it resolves through the registry like any other function — the same `sqrt` a query would have used, rather than a second implementation.
`Sqrt` is not a DataFusion unary: it is what a stddev's finalize expression needs, and cuDF's `unary_operator::SQRT` is what the hardwired finalize already calls.
`Sqrt` is the one expression with no DataFusion unary behind it: it exists because a stddev's finalize needs it.
`Unbuilt` is a lane never entered — for a join that is still its build phase, since nothing has been set.
`[[a,b],[c]]` — the bracket style `partition_groups` renders in, so the two nestings a reader compares index for index also read alike.
`[group keys…, state columns…]` — what the aggregators produce, and where the state annotations live.
`[k, count, mean, m2]` — the Welford triple, in DataFusion's own state order and types.
`[k, sum(v), count(v)]` — the state an avg decomposes into, which is what a merge reads.
`[start, end)` of the probe child's subtree in pre-order.
`[start, end)` per node.
```text resident = Σ byte_size of driver-held in-flight batches + Σ cached resident_bytes() over live executors ``` The executor half is cached per slot and refreshed one instance at a time.
`abandoned` is what makes the arithmetic a law rather than an inequality: the merge consumed 4+4+0 of the loader's three lanes, and the 4 rows the early exit stranded are on the loader's own line.
`aggregate.cpp` discriminates on `null_exprs` rather than on the masks, so writing one without the other would run a plain group-by and answer with one set where the query asked for five.
`assert_type_accountable` recurses into them.
`batch_partitioned_driver` — the tree, the queues, the schedule, and everything cross-partition.
`budget` of `None` accounts without ever tripping.
`budget` of `None` accounts without ever tripping.
`budget` of `None` accounts without ever tripping.
`calling_lanes` is how many lanes make the call, not how many the node's output declares: an emitter reads one lane and scatters into four, so it calls from one while its tree line says four.
`check` trips on `value > budget`, so equality is the boundary rather than a near miss — and it is the off-by-one a user would find rather than a test.
`compact_bytes` is the held size that triggers a compaction; it comes from the budget rule, which is the driver's.
`compact_bytes` is the held-state size at which a compaction runs.
`conditional_inner_join` evaluates it per pair, or a cross join and a mask where it is not AST-able.
`consumed` is `None` for the calls that take no input — zero bytes would count a release that never happened, which is what the hold and release counts exist to notice.
`count(*)` over nothing is 0, and a query asking for it gets an answer.
`ddof` is not checked here because nothing at a merge names it: it separates the sample forms from the population ones and appears only in the finalize expression, which is built from the same spec that wrote the annotation.
`dim(k, label)` is the build side and `fact(fk, v)` the probe, the pair the matrix works its examples on.
`dim(k, label)` joined to `fact(fk, v)`, keeping one column of each side.
`dim(k, label)` joined to `fact(fk, v)`, the pair the capability matrix works its examples on.
`dim(k, label)` semi-joined to `fact(fk, v)`, keeping one build column.
`done` means no further batch can ever arrive on that slot: the producer finished and its queue for this lane is empty.
`estimated_max_resident_size` per node, indexed by post-order sequence.
`execute_node` erases every input handle it reads, and there is no copy symbol — `slice_handle` consumes its input too, so it moves rather than copies.
`execute_single_node` swaps it for a stream over the batches the call was handed, so what it holds is never read.
`false` when nothing is runnable, which is how a run ends.
`false` — the SQL default — means a NULL key matches nothing, `true` is what a set operation lowered to a join needs.
`fetch` is DataFusion's, replicated onto every stage of the decomposition, which is sound because the top n of a union is the top n of each part's top n.
`fetch` is the top-N within this batch, which is what makes a sort 1:1 per batch rather than an accumulator.
`fetch` on the wire:
`file_schema` is the projected fields and `projection` is left empty, which the C++ reads as "every column of the schema I was given".
`filter` is a residual over both sides, which is what moves a mode off the streaming path.
`finish_and_fetch` takes the executor by value, so on failure there is nothing left to hold a slot open — which is the same thing the query being over already means.
`generated` is private to this component, which is the whole reason the wall is drawn here: eleven files name flatc's output and use 73 of its types between them, so it can only be private if all eleven are inside with it.
`index` is its index in the file, which is what the scan passes to `set_row_groups` — survivors are post-pruning, so it is not the position in this slice.
`intermediate` is `[group keys…, state columns…]` — what the aggregators produce, and what a finalize expression reads.
`joined_key_distribution` cannot name it, so the node quietly declares no distribution at all and a co-partitioned join above it loses the claim it earned.
`k * 10 = v` reads both sides, which is what makes it a residual rather than something DataFusion would have pushed below the join.
`k > fk` pairs only `k=3` with the two `fk=2` rows.
`k=2` matches only the first batch and `k=1` and `k=3` match neither, so a per-batch answer and a finished one differ.
`k=2` matches the first probe batch and nothing in the second.
`keep[i] == false` => row group i cannot match the predicate (prune it).
`keys` is the schema of the probe keys a finishing join accumulates — the key project's output, which is the node's key columns and nothing else.
`length: u64::MAX` means to the end.
`m2` and `merge_m2` have none of their own: both are folded into the SQL aggregate whose state they are, above.
`merge_m2` is the reason `outputs` is a list — it returns its three state columns together.
`merge_m2`: state into state, which none of DataFusion's aggregate modes does.
`merge` is listed rather than derived: the rule would be "the same aggregator, except count merges by sum", and that exception is the whole content.
`none` where the run drained, stated rather than left to an absence, since a smaller number with no cause reads exactly like a plan that produced less.
`null_equals_null` reaches the finish join too, and it has to: the pass substitutes for a single legacy call, so a NULL key matching a NULL key there and nowhere else would make the answer depend on how many batches the probe arrived in.
`object_meta.location` is an object_store Path (leading '/' stripped); we assume a '/'-rooted LOCAL filesystem object store, which holds for the serialize-time / planner use here.
`offsets[rows]-offsets[0]` for offset layouts, or Σ value byte lengths for View layouts.
`out_decimal_precision`/`out_decimal_scale` stay at zero, and that is a decision.
`out_type` is DataFusion's declared output type. cuDF derives its own fixed-point result scale — division most visibly — so the declared one travels with the op.
`peacock::partitioning::spark_hash_partition` is the device's copy and the live conformance gate proves the two agree bit for bit, so a second spelling on this side would be a divergence nothing else could see: the rows would still be joined, in the wrong lanes, and every per-partition count would drift from its golden.
`per batch: execute_node(#3 CudfFilter, batch)`, the calls grouped under the pattern that drives them — which is how the mapping table reads them too, per probe call and at finish.
`plan_batch_partitioned` itself, not `validate` called directly.
`positions` index the declaring node's fields; `func` and `ddof` are carried so a merge can confirm it is merging the aggregate it thinks it is.
`post_order` is the node's position in a children-first walk, which is the address `RecipePlan` indexes by.
`root` as it ran: the early-exit marker, then one node per line with its per-batch record beneath it.
`rows_skipped` is the saving a limit buys, and it sits with `abandoned` because both are early-exit quantities: rows released without an unload call, on the node that released them.
`schema` is DataFusion's own for this projection — the types it coerced to, not a second derivation of them.
`schema` is what the node declares it produces, which is what prices the batch — the ABI reports rows and varlen content, and the fixed width per row is the schema's.
`scratch_bytes` is the measured transient; `None` when the run is not instrumented.
`seen` is how many rows of the stream have already gone past this node, which is a count across lanes and therefore the driver's rather than an executor's.
`single_partition_driver` — one lane of one lane-scoped node.
`skip..skip+fetch` over a one-lane stream of any number of batches.
`sources_of` allocates, and the readiness predicate asks for it on every step.
`sources_of` materialized once per out lane: the predicate asks for it on every step, and the trait's `Vec` would be an allocation each time.
`state` pairs each column's name suffix with the aggregator producing it, so a column and its aggregator cannot desync.
`stddev` is the sample form and `stddev_pop` the population one.
`stddev` is the sample form and `stddev_pop` the population one.
`target_partitions` requested from DataFusion.
`then`, not `then_some`: the subtraction is the answer only when it is in range, and an eager argument underflows on every batch of the skip prefix.
`tp8-standard`), and both halves are authoritative: the golden path AND the session config derive from the same label, so a mislabeled test runs the config it claims instead of silently diverging from its name.
`x IN (a, b)` becomes `(x = a) OR (x = b)`, and `NOT IN` its negation: cuDF's AST has no IN opcode, so the lowering has to happen somewhere and the IR is where this mode can state it.
avg decomposes into two columns and this merge reads only one of them, so the count would arrive at the finalize unmerged.
avg decomposes into two columns, so an input declaring one of them has either lost a column or annotated a different aggregate's state.
c_custkey rises with the row groups, so a predicate past the first group's maximum leaves the second — and the index it keeps is the file's, not a position in the survivor list.
coalesce is the shape that makes a general rule wrong here, so this does not guess: a function's result is possibly-NULL.
cuDF's own rule for a decimal divide gives scale s_l - s_r, which is 0 here; what travels is the scale DataFusion derived.
customer is 150k rows and supplier 10k; a proportional split would hand the most budget to the source already producing the most bytes.
customer is two row groups at this scale, the second one short.
customer's key column reads several hundred KB at the same threshold, and gets the whole sequence.
flatc's output, included verbatim: 7,336 lines, and the whole reason `wire/` is drawn where it is.
mark_done_and_fetch and friends consume the executor, so there is nothing left to ask — the slot goes to zero rather than keeping its last figure.
merge_m2 returns its three state columns together, and the line spells them out.
n_name is field 1 of the file schema, and its chunks are what a scan projecting it reads — not a width derived from the type, which is the whole point of measuring the file.
nation reads a couple of KB, under the threshold: one lane, so nothing to merge back and no shuffle to re-land rows the lane never split.
offsets[rows]-offsets[0]; offsets are i32 (Utf8/Binary) or i64 (Large*).
one entry per aggregate whose state this output carries
ordinals of the group-by keys, including a synthesized `__grouping_id`
per join, lanes that have yet to leave their build phase
per node, how many of its lanes can step.
rank -> node, sorted by (height, index): the first set bit is the pick.
region is the build side — DataFusion put the smaller table there and flipped the predicate — and both sides' ordinal 0 is a different column, which is what makes a mix-up visible.
root(0) over two subtrees: 1 -> 2 and 3 -> 4.
scan -> merge -> scatter -> merge -> unload: a shuffle whose lane count is the shape's, so every layout runs the same plan over the same rows.
scatter over merge over scatter: two shuffles live at the same time, which is a different bound from two joins — nothing here is held, so every queue has to be drained by height alone.
tp4 with batching on, which is what makes the small-table threshold bite.
tpch.minimal has no NULLs anywhere, so every column reads as not-nullable here however it is declared — which is the point: the declaration says nothing.
unload <- join <- [coalesce <- merge_sorted <- source(build, 2), emit(4) <- source(probe, 1)] One of everything the counts below distinguish: a join with two children, a partition accumulator with two input lanes, an emitter reading one lane and filling four, and a lane-scoped node on each side of it.
unload <- join <- [coalesce <- source(build), filter <- source(probe)]
unload(0) <- join(1) <- [build 2 <- 3, probe 4 <- 5]
unload(0) <- outer join(1) <- [build 2, probe 3 = inner join <- [build 4, probe 5]]
unload(0) <- outer(1) <- [build 2 = inner join <- [3, 4], probe 5]
xorshift64*, seeded and fixed: the determinism rules apply to tests too.
Σ over the accumulators — held whatever the batch size, so it is spent first.
Σ var-length CONTENT bytes across all columns of one batch — the data-dependent term of [`logical_size_from_schema`], and the CPU's spelling of what a device reports as `varlen_content_bytes`.
